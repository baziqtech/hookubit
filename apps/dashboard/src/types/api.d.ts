/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/v1/auth/register": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create an account and its organization
         * @description Refused with `forbidden` unless ALLOW_OPEN_REGISTRATION=true. Otherwise always 202, whether or not the address is already registered - a 201/409 split let an attacker enumerate accounts. No session cookie is set: the user verifies the address from the email and then logs in. A taken address gets a "someone tried to register" notice instead.
         */
        post: operations["AuthController_register"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Exchange credentials for a session cookie
         * @description Returns `unauthenticated` for unknown, disabled and wrong-password alike. A correct password on an account whose address was never confirmed returns `email_not_verified` instead - only reachable once the password is already known, so it enumerates nothing.
         */
        post: operations["AuthController_login"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Clear the session cookie
         * @description Idempotent.
         */
        post: operations["AuthController_logout"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/verify-email": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Consume a single-use email verification token */
        post: operations["AuthController_verifyEmail"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/resend-verification": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request a fresh email verification link
         * @description ALWAYS 202, with an identical body: whether or not the address is registered, whether or not it is already verified, whether or not the account is disabled, and whether or not the mail transport is up. A degraded mailer must not turn this into an oracle - the same trap that was found and fixed in forgot-password. Any previous verification link is invalidated, so the newest email is the one that works.
         */
        post: operations["AuthController_resendVerification"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/forgot-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request a password reset link
         * @description Always 202, whether or not the address is registered.
         */
        post: operations["AuthController_forgotPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/reset-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Consume a single-use reset token and set a new password
         * @description Clears any existing session; the user must log in again.
         */
        post: operations["AuthController_resetPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/onboarding-completed": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mark the product tour as finished or skipped
         * @description Idempotent: completing twice is a 204, not a conflict, and the recorded instant is the FIRST completion - it never drifts forward on a replay. Acts only on the calling user's own row; there is no body and no path parameter, so one account cannot complete another's. Read the resulting instant back from `user.onboarding_completed_at` on GET /v1/auth/session.
         */
        post: operations["AuthController_completeOnboarding"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/session": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Return the user behind the current session cookie */
        get: operations["AuthController_session"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the organizations the caller belongs to
         * @description Scoped to the session user. Soft-deleted organizations are omitted, because every route under them already answers 404. Paged with the canonical envelope `{ data, has_more, next_offset }` - BREAKING: `total`, `limit` and `offset` were removed. A `total` cost a second COUNT per request and, taken at a different instant from the rows, could not be used to derive the last page reliably; `has_more` states it. `next_offset` is null - never absent, never 0 - on the last page.
         */
        get: operations["OrganizationsController_list"];
        put?: never;
        /**
         * Create an organization
         * @description The caller becomes its owner. The organization, the owner membership and the audit row are written in one transaction. An omitted slug is derived from the name and suffixed if it is taken; an explicitly supplied slug that is taken returns 409. Rate limited, and capped per account.
         */
        post: operations["OrganizationsController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations/{orgId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Read one organization */
        get: operations["OrganizationsController_get"];
        put?: never;
        post?: never;
        /**
         * Soft-delete an organization
         * @description Owner only, and never a hard delete: the delivery ledger hangs off this chain and its foreign keys are ON DELETE RESTRICT. Sets status=deleted, after which every route under the organization answers 404 for every member.
         */
        delete: operations["OrganizationsController_remove"];
        options?: never;
        head?: never;
        /**
         * Rename an organization or change its slug
         * @description Status is not settable here; suspension is a platform decision.
         */
        patch: operations["OrganizationsController_update"];
        trace?: never;
    };
    "/v1/organizations/{orgId}/members": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the members of an organization
         * @description Paged with the canonical envelope `{ data, has_more, next_offset }` - BREAKING: `total`, `limit` and `offset` were removed. Read `has_more`; `next_offset` is null - never absent, never 0 - on the last page.
         */
        get: operations["MembersController_list"];
        put?: never;
        /**
         * Invite someone to the organization
         * @description Always 202, whether the address is already a member, already has an account, or is unknown - anything else would let a member enumerate the platform. No membership is created here: the invitee redeems a single-use token from their own mailbox at POST /v1/invitations/accept. 403 if the requested role is above the caller's own rank.
         */
        post: operations["MembersController_invite"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations/{orgId}/members/{memberId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Remove a member
         * @description Removal is a role change to "no role", so the same lattice applies - including the last-owner rule.
         */
        delete: operations["MembersController_remove"];
        options?: never;
        head?: never;
        /**
         * Change a member's role
         * @description Enforced by the role lattice: never your own membership, never a role above your own rank, never a member who outranks you, and never the last owner. The owner count is taken inside the same transaction as the write.
         */
        patch: operations["MembersController_changeRole"];
        trace?: never;
    };
    "/v1/invitations/accept": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Redeem a membership invitation
         * @description Requires a session: the token proves possession of a mailbox, the session proves who is asking, and the two must be the same address. Single-use, and refused if the inviting member no longer holds the rank to grant that role. Redeeming an invitation you have already accepted returns the organization unchanged rather than altering your role.
         */
        post: operations["InvitationsController_accept"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations/{orgId}/projects": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the projects in an organization
         * @description Newest first. Soft-deleted projects are hidden unless `status=deleted`. Paged with the canonical envelope `{ data, has_more, next_offset }` - BREAKING: `count` was removed. Read `has_more`, never a row count compared against `limit`, and pass `next_offset` back as `offset` to continue; it is null on the last page.
         */
        get: operations["ProjectsController_list"];
        put?: never;
        /**
         * Create a project
         * @description The `environment` chosen here is permanent. `slug` defaults to a normalised form of `name` and must be unique within the organization. Rate limited, and subject to a per-organization ceiling (`MAX_PROJECTS_PER_ORGANIZATION`) that counts live projects only.
         */
        post: operations["ProjectsController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations/{orgId}/projects/{projectId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one project */
        get: operations["ProjectsController_get"];
        put?: never;
        post?: never;
        /**
         * Soft-delete a project
         * @description Sets `status = deleted`. Nothing is erased: endpoints, API keys and the whole delivery ledger survive, the project stops being visible to this API, and the ingest path refuses its API keys. The slug stays taken. Returns the project in its deleted state.
         */
        delete: operations["ProjectsController_remove"];
        options?: never;
        head?: never;
        /**
         * Rename a project or change its slug
         * @description `name` and `slug` only. `environment` is immutable and `status` is not editable here (use DELETE); both are refused with `invalid_request` rather than ignored.
         */
        patch: operations["ProjectsController_update"];
        trace?: never;
    };
    "/v1/projects/{projectId}/api-keys": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the API keys in a project
         * @description Newest first. Revoked and expired keys are included and labelled by `status`; the secret is never returned here, only `key_prefix`. Paged with the canonical envelope `{ data, has_more, next_offset }` - BREAKING: `count` was removed. Read `has_more` before concluding you have seen every credential in the project, and pass `next_offset` back as `offset` to continue; it is null on the last page.
         */
        get: operations["ApiKeysController_list"];
        put?: never;
        /**
         * Issue an API key
         * @description THE ONLY RESPONSE THAT EVER CONTAINS THE PLAINTEXT KEY. Only its SHA-256 hash is stored, so it cannot be shown again by this or any other endpoint - show it to the user once and let them copy it. The environment is taken from the project (wk_test_ for a test project, wk_live_ for a live one) and cannot be chosen. Rate limited, and subject to a per-project ceiling (`MAX_API_KEYS_PER_PROJECT`) that counts un-revoked keys.
         */
        post: operations["ApiKeysController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/api-keys/{apiKeyId}/revoke": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke an API key
         * @description Takes effect immediately: the ingest path refuses the key on its next request. The row is kept with `revoked_at` set, so the delivery history it produced stays attributable. Idempotent - revoking an already-revoked key returns it unchanged.
         */
        post: operations["ApiKeysController_revoke"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/endpoints": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the endpoints in a project
         * @description Soft-deleted endpoints are hidden unless asked for; they are never erased. Paged with the canonical envelope `{ data, has_more, next_offset }`; `next_offset` is null on the last page.
         *
         *     Every row carries `has_live_secret`, answered for the whole page in one grouped read - so a UI can tell which paused endpoints can actually be resumed without asking per endpoint.
         */
        get: operations["EndpointsController_list"];
        put?: never;
        /**
         * Create an endpoint and its first signing secret
         * @description The URL is checked for schemes, embedded credentials and literal private, loopback, link-local or cloud-metadata addresses so an unusable URL is refused now rather than becoming silent delivery failures later. A version 1 signing secret is minted with the endpoint - it is returned in this response only, and only to a caller who also holds `endpoint-secrets.write` (owner or admin).
         */
        post: operations["EndpointsController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/endpoints/{endpointId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch one endpoint
         * @description Returns soft-deleted endpoints too, with `status: "deleted"`, so a delivery in the ledger that points at a removed endpoint is still readable. `has_live_secret` is the same answer the listing gives for this endpoint.
         */
        get: operations["EndpointsController_get"];
        put?: never;
        post?: never;
        /**
         * Remove an endpoint (soft delete)
         * @description Sets `status` to `deleted`. The row is kept forever: `deliveries.endpoint_id` is ON DELETE RESTRICT precisely so that removing an endpoint cannot erase the record of what it was sent. Idempotent.
         */
        delete: operations["EndpointsController_remove"];
        options?: never;
        head?: never;
        /**
         * Update an endpoint
         * @description `status` is not writable here. Enabling, disabling and deleting have their own routes because each has a precondition a PATCH would walk past.
         */
        patch: operations["EndpointsController_update"];
        trace?: never;
    };
    "/v1/projects/{projectId}/endpoints/{endpointId}/enable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resume deliveries to an endpoint
         * @description Refused when the endpoint has no active signing secret: the data plane fails closed rather than delivering unsigned, so enabling would only queue failures.
         *
         *     **Check `has_live_secret` before offering this.** It is the same condition, evaluated the same way, and it is false on a normal, expected state - an endpoint created by a `developer` stays paused with `secret_pending` because `endpoint-secrets.*` is owner/admin only. Offering the action there is offering a guaranteed 409; the operator wants "rotate a secret first", not a refusal after the click.
         */
        post: operations["EndpointsController_enable"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/endpoints/{endpointId}/disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Pause deliveries to an endpoint
         * @description Queued deliveries are not discarded. The circuit breaker`s own `disabled_reason` and `disabled_at` are left untouched; the reason given here goes to the audit log.
         */
        post: operations["EndpointsController_disable"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/endpoints/{endpointId}/secrets": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List signing secret metadata
         * @description Metadata only. No response on this route can contain a plaintext secret - the response type has no field it could occupy. A secret is shown exactly once, when it is created.
         */
        get: operations["EndpointSecretsController_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/endpoints/{endpointId}/secrets/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue a new signing secret, keeping the old one valid
         * @description The new secret becomes active immediately and the current ones keep signing until `overlap_seconds` (default 86400) elapses. During that window every delivery carries one `v1=` signature per active secret and a consumer that verifies with either one succeeds, so consumers can be rolled without dropping a delivery. The plaintext is in this response and nowhere else, ever.
         */
        post: operations["EndpointSecretsController_rotate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/endpoints/{endpointId}/secrets/{secretId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Stop one secret signing
         * @description Refused when it is the only secret still signing for a live endpoint: that state makes every delivery fail closed rather than go out unsigned. To retire a leaked secret immediately, rotate with `overlap_seconds: 0`.
         */
        delete: operations["EndpointSecretsController_revoke"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/subscriptions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the subscriptions in a project
         * @description Newest first. `event_types` comes back exactly as it was stored. The envelope is `{ data, has_more, next_offset }` - there is no `total`, because it would cost a second COUNT on every request and a client pages on `has_more`. Read `has_more` before concluding you have seen every route out of this project, and pass `next_offset` back as `offset` to continue.
         */
        get: operations["WebhookSubscriptionsController_list"];
        put?: never;
        /**
         * Subscribe an endpoint to a set of event types
         * @description The filter is stored EXACTLY as sent. A pattern the router cannot honour is a 400 - it is never coerced to "*", and an empty `event_types` array is refused rather than guessed at. Subject to a per-project ceiling (`MAX_SUBSCRIPTIONS_PER_PROJECT`), enforced inside a serializable transaction because each subscription multiplies the deliveries one event produces.
         */
        post: operations["WebhookSubscriptionsController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/subscriptions/{subscriptionId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one subscription */
        get: operations["WebhookSubscriptionsController_get"];
        put?: never;
        post?: never;
        /**
         * Remove a subscription (hard delete)
         * @description The row is really deleted, unlike endpoints and projects. Nothing has a foreign key to `webhook_subscriptions`, the delivery ledger keeps its own `endpoint_id` and `event_id`, and the whole routing rule is written to the audit log on the way out so a historical delivery's `subscription_id` stays explainable. Idempotent: deleting an already-deleted subscription still answers 204.
         */
        delete: operations["WebhookSubscriptionsController_remove"];
        options?: never;
        head?: never;
        /**
         * Update a subscription
         * @description `event_types` and `payload_filter` are REPLACED wholesale, not merged. `enabled` is not writable here - enabling and disabling have their own routes so that pausing a route is a distinct, separately auditable act rather than a field riding along in a rename. Both sides of a filter change are recorded in the audit log.
         */
        patch: operations["WebhookSubscriptionsController_update"];
        trace?: never;
    };
    "/v1/projects/{projectId}/subscriptions/{subscriptionId}/enable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resume matching for a subscription
         * @description Idempotent. The filter is untouched.
         */
        post: operations["WebhookSubscriptionsController_enable"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/subscriptions/{subscriptionId}/disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Stop matching, keep the filter
         * @description A disabled subscription is skipped before the event-type test, so it matches nothing at all. This - not an empty `event_types` array - is how you stop deliveries down one route. Deliveries already queued are not discarded. Idempotent; the reason goes to the audit log.
         */
        post: operations["WebhookSubscriptionsController_disable"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/retry-policies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List the retry policies in a project */
        get: operations["RetryPoliciesController_list"];
        put?: never;
        /**
         * Create a retry policy
         * @description Every field is bounded to something the delivery workers can compute a positive, finite delay from — an unbounded `max_delay_ms` is what once overflowed the computed duration and scheduled retries permanently in the past. The FIRST policy in a project becomes its default automatically.
         */
        post: operations["RetryPoliciesController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/retry-policies/{policyId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one retry policy */
        get: operations["RetryPoliciesController_get"];
        put?: never;
        post?: never;
        /**
         * Delete a retry policy
         * @description Refused while any live endpoint references it: `endpoints.retry_policy_id` is ON DELETE SET NULL, so the delete would silently move those endpoints onto the platform default backoff. Deleting the project default also requires `?replacement_id=`, unless it is the only policy left.
         */
        delete: operations["RetryPoliciesController_remove"];
        options?: never;
        head?: never;
        /**
         * Update a retry policy
         * @description `is_default` is not writable here — it has its own route, because "exactly one default per project" is a property of a set of rows and needs a transaction that clears the old one. The patch is validated MERGED with the stored row, so a single field cannot walk the policy into an incoherent combination.
         */
        patch: operations["RetryPoliciesController_update"];
        trace?: never;
    };
    "/v1/projects/{projectId}/retry-policies/{policyId}/default": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Make this the project default retry policy
         * @description Clears the previous default and sets this one inside a single SERIALIZABLE transaction. Two concurrent calls cannot both win: the schema has no partial unique index, so nothing else would stop a project ending up with two defaults.
         */
        post: operations["RetryPoliciesController_setDefault"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/rate-limits": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List the rate-limit policies in a project */
        get: operations["RateLimitsController_list"];
        put?: never;
        /**
         * Create a rate-limit policy
         * @description One policy per `(scope, resource_id)`, including the `resource_id: null` row that means "every resource in this scope". A non-null `resource_id` is resolved through the scoped repository for whatever the scope names, so another tenant’s endpoint is a 404.
         */
        post: operations["RateLimitsController_create"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/rate-limits/{policyId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one rate-limit policy */
        get: operations["RateLimitsController_get"];
        put?: never;
        post?: never;
        /**
         * Delete a rate-limit policy
         * @description A hard delete: nothing in the delivery ledger references a rate-limit policy. Removing the last policy covering a resource means it falls back to the next scope up.
         */
        delete: operations["RateLimitsController_remove"];
        options?: never;
        head?: never;
        /**
         * Update a rate-limit policy
         * @description `scope` and `resource_id` are the row’s identity; changing either re-resolves the resource and re-checks uniqueness in the same transaction.
         */
        patch: operations["RateLimitsController_update"];
        trace?: never;
    };
    "/v1/projects/{projectId}/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List events in a project
         * @description Filter by event type, ingest status, date range, and a free-text fragment of the idempotency key. Payloads are NOT included - `events` is the largest table in the system and its rows are raw request bodies; fetch one event to see one payload. Read the parameter descriptions before running `status` or `idempotency_key` on a busy project: both are scans of whatever the indexed filters left.
         */
        get: operations["EventsController_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/events/{eventId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch one event, with its payload
         * @description The payload is returned as `payload.body` - the AUTHORITATIVE raw bytes, decoded: what was hashed and what was signed. `payload.normalised_json` is the jsonb copy kept for filtering and is explicitly NOT what was delivered, because PostgreSQL normalises jsonb. A payload held in object storage is reported as such, with its location, rather than returned as an empty body.
         */
        get: operations["EventsController_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/events/{eventId}/deliveries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the deliveries this event fanned out to
         * @description The "did finance ever receive this?" route: one row per endpoint the event was materialised for, each with its own status, attempt count and next attempt. Accepts the same filters as the deliveries listing (the event is forced, so `event_id` in the query is ignored).
         */
        get: operations["EventsController_deliveries"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/events/{eventId}/replay": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Replay an event to one endpoint, or to all originally matched endpoints
         * @description "Originally matched" is read off the EXISTING delivery rows, never by re-running the subscription match - subscriptions change, and re-matching would deliver to endpoints that were never targeted and skip ones that were. Every replay is a NEW delivery row carrying `replay_of_delivery_id` and `replayed_by`; no original row and no attempt is modified. An endpoint that has since been deleted or disabled is refused with a 409 rather than queued to nowhere.
         */
        post: operations["EventsController_replay"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/deliveries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List deliveries in a project
         * @description Filter by status, endpoint, event, event type and date range, or by `failing_now` - everything that has failed and not recovered. Every filter is either index-supported or documented as a scan; see the parameter descriptions before running one on a busy project. Replays are included by default: a replay is a real delivery, and hiding it would make the ledger lie.
         */
        get: operations["DeliveriesController_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/deliveries/{deliveryId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch one delivery with its full attempt history
         * @description The single most useful response in this API: the delivery, the event it came from, the endpoint as it stands now, and every attempt in order - number, HTTP status, duration, request and response headers, the truncated response body, the error code and message, and when the next attempt is due. Credential-shaped request header VALUES are redacted; the signature header is not.
         */
        get: operations["DeliveriesController_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/deliveries/{deliveryId}/attempts": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Page the attempt history
         * @description For a delivery with more attempts than `GET /deliveries/:id` inlines. Ascending by attempt number. `delivery_attempts` is append-only, so a page fetched twice is identical unless a new attempt has been made.
         */
        get: operations["DeliveriesController_attempts"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/deliveries/{deliveryId}/replay": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Replay one delivery
         * @description Creates a NEW delivery row carrying `replay_of_delivery_id` and `replayed_by`. The original is not modified in any way - not its status, not its attempt count, and not one row of its append-only attempt history. Refused with a 409 when the endpoint has since been deleted or disabled, because such a replay would be abandoned by the worker rather than delivered. Replaying a replay is allowed and chains.
         */
        post: operations["DeliveriesController_replay"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations/{orgId}/audit-logs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the organization’s audit trail
         * @description Newest first, scoped to this organization by a predicate in the WHERE clause rather than a check after the read. Filters: `user_id`, `action`, `resource_type`, `resource_id` and the `created_after`/`created_before` range. `action` and the date range are index-supported; `user_id`, `resource_type` and `resource_id` are SCANS within the organization and date window - pair them with `created_after`. Paged with the canonical envelope `{ data, has_more, next_offset }`; `next_offset` is null - never absent, never 0 - on the last page. `metadata` is served exactly as it was stored: values redacted at write time stay redacted, and there is no filter over metadata contents, because one would be an oracle against that redaction.
         */
        get: operations["AuditLogsController_list"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/organizations/{orgId}/audit-logs/{auditLogId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch one audit row
         * @description For deep-linking out of the list. Read-only, like everything on this controller: an audit row is never edited or removed, and a correction is a new row.
         */
        get: operations["AuditLogsController_get"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/analytics/deliveries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Delivery outcomes over a window, beside the window before it
         * @description Every `DeliveryStatus` counted exactly, rolled up into succeeded / failing / exhausted / in-flight, with `success_rate` over SETTLED deliveries only. The same numbers are returned for the immediately preceding window of equal length, so "is it getting worse?" is one request rather than two and a subtraction. `success_rate` is NULL, never 0, when nothing settled: 0 means everything failed.
         */
        get: operations["AnalyticsController_deliveries"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/analytics/endpoints": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Which endpoints are failing, ranked worst first
         * @description Ranked by `failed + exhausted` in the window, with the per-status split, the total delivered to each, and a failure rate. Read the rate beside the count: one endpoint at 100% of two deliveries is not the outage. The current endpoint name, URL, status and `enabled` flag are included so a disabled endpoint is not mistaken for a broken one.
         */
        get: operations["AnalyticsController_endpoints"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/analytics/latency": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Attempt latency percentiles (p50/p95/p99) from delivery_attempts.duration_ms
         * @description Computed over a BOUNDED SAMPLE of the most recent measured attempts in the window. `exact` says whether the sample was the whole window; when it is false the numbers describe the most recent traffic in the window, and `sample_size` says how much was measured. Nearest-rank, so every value returned is a duration something actually took. An exact percentile needs `percentile_cont`, which is raw SQL - see HANDOFF.md.
         */
        get: operations["AnalyticsController_latency"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/{projectId}/analytics/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Event volume over a window, with the busiest event types
         * @description Events PUBLISHED in the window - not deliveries. One event fans out to one delivery per matching subscription, so these two numbers are expected to differ and their ratio is the project's fan-out. The preceding window of equal length is returned alongside.
         */
        get: operations["AnalyticsController_events"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        RegisterDto: {
            /** @example ada@example.com */
            email: string;
            /** @description Never logged, never returned. */
            password: string;
            name?: string;
            /** @description Organization created for this user. Defaults to the local part of the email. */
            organization_name?: string;
        };
        AcknowledgedDto: {
            /**
             * @description Deliberately uninformative: password reset and verification responses must not reveal whether an account exists.
             * @example accepted
             */
            status: string;
        };
        LoginDto: {
            /** @example ada@example.com */
            email: string;
            /** @description Never logged, never returned. */
            password: string;
        };
        AuthUserDto: {
            /** @example usr_01J... */
            id: string;
            /** @example ada@example.com */
            email: string;
            name?: Record<string, never> | null;
            /** @description False until the verification token is presented. */
            email_verified: boolean;
            /**
             * Format: date-time
             * @description ISO-8601 instant at which the user finished OR skipped the product tour; null if neither. Carried on every response that returns a user - session, login, verify-email - so the client never needs a second request to decide whether to show the tour. Set by POST /v1/auth/onboarding-completed.
             * @example 2026-09-08T14:20:00.000Z
             */
            onboarding_completed_at?: Record<string, never> | null;
        };
        SessionResponseDto: {
            user: components["schemas"]["AuthUserDto"];
        };
        VerifyEmailDto: {
            /** @description Single-use token from the verification email. */
            token: string;
        };
        ResendVerificationDto: {
            /** @example ada@example.com */
            email: string;
        };
        ForgotPasswordDto: {
            /** @example ada@example.com */
            email: string;
        };
        ResetPasswordDto: {
            /** @description Single-use token from the password reset email. */
            token: string;
            password: string;
        };
        OrganizationDto: {
            /** @example org_01J... */
            id: string;
            /** @example Acme */
            name: string;
            /**
             * @description Globally unique, URL-safe.
             * @example acme
             */
            slug: string;
            /**
             * @example active
             * @enum {string}
             */
            status: "active" | "suspended" | "deleted";
            /**
             * @description The requesting user's role in this organization.
             * @enum {string}
             */
            role: "owner" | "admin" | "developer" | "viewer" | "billing";
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        OrganizationListDto: {
            data: components["schemas"]["OrganizationDto"][];
            /**
             * @description True when more organizations match than this page carries. Read this, never a row count compared against `limit`, to decide whether you have seen them all.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass back as `offset` to fetch the next page. NULL - never absent, never 0 - when this page was the last one, so a client branches on one thing.
             * @example null
             */
            next_offset: number | null;
        };
        CreateOrganizationDto: {
            /** @example Acme */
            name: string;
            /**
             * @description Lowercase letters, digits and single dashes. Derived from the name when omitted, with a random suffix if that name is taken.
             * @example acme
             */
            slug?: string;
        };
        UpdateOrganizationDto: {
            name?: string;
            /** @description Lowercase letters, digits and single dashes. Derived from the name when omitted, with a random suffix if that name is taken. */
            slug?: string;
        };
        MemberDto: {
            /** @example mem_01J... */
            id: string;
            /** @example usr_01J... */
            user_id: string;
            /** @example ada@example.com */
            email?: Record<string, never> | null;
            name?: Record<string, never> | null;
            /** @enum {string} */
            role: "owner" | "admin" | "developer" | "viewer" | "billing";
            /** @description True when the account has been disabled platform-wide. */
            disabled: boolean;
            /** Format: date-time */
            created_at: string;
        };
        MemberListDto: {
            data: components["schemas"]["MemberDto"][];
            /**
             * @description True when more members exist in this organization than the page carries.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass back as `offset` to fetch the next page. NULL - never absent, never 0 - on the last page.
             * @example null
             */
            next_offset: number | null;
        };
        InviteMemberDto: {
            /** @example ada@example.com */
            email: string;
            /**
             * @description Must be a role the caller may assign: never above their own rank. An admin cannot invite an owner.
             * @enum {string}
             */
            role: "owner" | "admin" | "developer" | "viewer" | "billing";
        };
        InvitationAcceptedDto: {
            /**
             * @description Deliberately uninformative. The response is identical whether the address is already a member, has an account, or has none - anything else would let a member enumerate the platform.
             * @example accepted
             */
            status: string;
        };
        UpdateMemberRoleDto: {
            /**
             * @description Enforced by the role lattice: never your own membership, never a role above your own rank, never someone who outranks you, and never the last owner.
             * @enum {string}
             */
            role: "owner" | "admin" | "developer" | "viewer" | "billing";
        };
        AcceptInvitationDto: {
            /** @description The single-use token from the invitation email. */
            token: string;
        };
        AcceptedInvitationDto: {
            /** @description The organization the caller has just joined, with their new role. */
            organization: components["schemas"]["OrganizationDto"];
        };
        /** @enum {string} */
        ProjectStatus: "active" | "suspended" | "deleted";
        /** @enum {string} */
        Environment: "test" | "live";
        ProjectDto: {
            /** @example proj_01J8ZK... */
            id: string;
            /** @example org_01J8ZK... */
            organization_id: string;
            /** @example Payments */
            name: string;
            /**
             * @description Unique within the organization. Deleted projects keep theirs.
             * @example payments
             */
            slug: string;
            /** @description IMMUTABLE after creation. It selects which API keys (wk_live_/wk_test_) and which ingest traffic belong to this project, so changing it would silently re-scope every key and endpoint underneath it. */
            environment: components["schemas"]["Environment"];
            /** @description `deleted` is a soft delete: the row and its delivery ledger survive, but the project is invisible to this API and the ingest path refuses its API keys. */
            status: components["schemas"]["ProjectStatus"];
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        ProjectListDto: {
            data: components["schemas"]["ProjectDto"][];
            /**
             * @description True when more projects match this filter than the page carries. The bound was reached; fetch `next_offset` to continue.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass as `offset` to fetch the next page. Null when this page was the last one.
             * @example null
             */
            next_offset: number | null;
        };
        CreateProjectDto: {
            /** @example Payments */
            name: string;
            /**
             * @description Lowercase letters, digits and single hyphens. Unique within the organization. Derived from `name` when omitted; a supplied slug is validated, never rewritten.
             * @example payments
             */
            slug?: string;
            /**
             * @description CANNOT BE CHANGED LATER. Defaults to `test`, so a project is never created live by omission. API keys minted here must carry the matching prefix (wk_test_/wk_live_) and the ingest path re-checks the pair on every request.
             * @default test
             */
            environment: components["schemas"]["Environment"];
        };
        UpdateProjectDto: {
            /** @example Payments */
            name?: string;
            /**
             * @description Unique within the organization; a collision answers 409 `conflict`.
             * @example payments
             */
            slug?: string;
        };
        /** @enum {string} */
        MemberRole: "owner" | "admin" | "developer" | "viewer" | "billing";
        ApiKeyDto: {
            /** @example key_01J8ZK... */
            id: string;
            /** @example proj_01J8ZK... */
            project_id: string;
            /** @example Payments ingest (production) */
            name: string;
            /**
             * @description The first 12 characters of the key. Safe to display and to log; never enough to reconstruct the credential.
             * @example wk_live_a9Kd
             */
            key_prefix: string;
            /** @description Always equal to the project's environment; the ingest path re-checks the pair. */
            environment: components["schemas"]["Environment"];
            /**
             * @description Derived from `revoked_at`/`expires_at` at read time, exactly as the ingest path derives it. `active` here still does not mean the key works: a suspended or deleted project refuses every key under it.
             * @enum {string}
             */
            status: "active" | "expired" | "revoked";
            /** @description The scopes this key was MINTED with: a snapshot of its issuer's authority at that instant, which nothing re-checks. Read `effective_scopes` to find out what the key may do now. The ingest path does NOT consult scopes today - it authenticates on the key, its project and its environment - so an empty list is a normal ingest key. */
            scopes: string[];
            /** @description WHAT THIS KEY MAY ACTUALLY DO: `scopes` intersected with the permissions its issuer holds RIGHT NOW. A key minted by a developer who has since been demoted to viewer, or removed from the organization, reports fewer scopes here than it was minted with - and an EMPTY list once the issuer is gone entirely. This is the authoritative list for any authorization decision; `scopes` is history. */
            effective_scopes: string[];
            /**
             * @description WHO MINTED THIS KEY. Taken from the resolved session at creation, never from the request body. Null for a key minted before the column existed, or whose user row has been deleted.
             * @example usr_01J8ZK...
             */
            created_by_user_id?: Record<string, never> | null;
            /**
             * @description The issuer's membership, which is what the effective-scope derivation joins to. It goes NULL when the membership is removed, and that going NULL is itself the signal that the issuer has left - at which point `effective_scopes` is empty.
             * @example mem_01J8ZK...
             */
            created_by_membership_id?: Record<string, never> | null;
            /** @description The issuer's role AS IT IS NOW, not as it was at mint time. Null when the issuer is no longer a member. This is the role `effective_scopes` was derived from. */
            created_by_role?: components["schemas"]["MemberRole"] | null;
            /** Format: date-time */
            expires_at?: Record<string, never> | null;
            /**
             * Format: date-time
             * @description Best-effort, written by the data plane. Never a basis for an authorization call.
             */
            last_used_at?: Record<string, never> | null;
            /** Format: date-time */
            revoked_at?: Record<string, never> | null;
            /** Format: date-time */
            created_at: string;
        };
        ApiKeyListDto: {
            data: components["schemas"]["ApiKeyDto"][];
            /**
             * @description True when more keys exist in this project than the page carries.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass as `offset` to fetch the next page. Null when this page was the last one.
             * @example null
             */
            next_offset: number | null;
        };
        CreateApiKeyDto: {
            /**
             * @description How an operator will recognise this key in the list. Shown in audit entries.
             * @example Payments ingest (production)
             */
            name: string;
            /**
             * Format: date-time
             * @description Must be in the future. The key stops authenticating at this instant with no further action; omit for a key that only revocation ends.
             * @example 2027-01-01T00:00:00.000Z
             */
            expires_at?: string;
            /**
             * @description Control-plane permissions this key may hold. Each must be a real permission AND one the caller holds themselves - a key cannot be minted with more authority than the person minting it. Defaults to none, which is what an ingest-only key wants.
             * @example [
             *       "events.read"
             *     ]
             */
            scopes?: string[];
        };
        CreatedApiKeyDto: {
            /** @example key_01J8ZK... */
            id: string;
            /** @example proj_01J8ZK... */
            project_id: string;
            /** @example Payments ingest (production) */
            name: string;
            /**
             * @description The first 12 characters of the key. Safe to display and to log; never enough to reconstruct the credential.
             * @example wk_live_a9Kd
             */
            key_prefix: string;
            /** @description Always equal to the project's environment; the ingest path re-checks the pair. */
            environment: components["schemas"]["Environment"];
            /**
             * @description Derived from `revoked_at`/`expires_at` at read time, exactly as the ingest path derives it. `active` here still does not mean the key works: a suspended or deleted project refuses every key under it.
             * @enum {string}
             */
            status: "active" | "expired" | "revoked";
            /** @description The scopes this key was MINTED with: a snapshot of its issuer's authority at that instant, which nothing re-checks. Read `effective_scopes` to find out what the key may do now. The ingest path does NOT consult scopes today - it authenticates on the key, its project and its environment - so an empty list is a normal ingest key. */
            scopes: string[];
            /** @description WHAT THIS KEY MAY ACTUALLY DO: `scopes` intersected with the permissions its issuer holds RIGHT NOW. A key minted by a developer who has since been demoted to viewer, or removed from the organization, reports fewer scopes here than it was minted with - and an EMPTY list once the issuer is gone entirely. This is the authoritative list for any authorization decision; `scopes` is history. */
            effective_scopes: string[];
            /**
             * @description WHO MINTED THIS KEY. Taken from the resolved session at creation, never from the request body. Null for a key minted before the column existed, or whose user row has been deleted.
             * @example usr_01J8ZK...
             */
            created_by_user_id?: Record<string, never> | null;
            /**
             * @description The issuer's membership, which is what the effective-scope derivation joins to. It goes NULL when the membership is removed, and that going NULL is itself the signal that the issuer has left - at which point `effective_scopes` is empty.
             * @example mem_01J8ZK...
             */
            created_by_membership_id?: Record<string, never> | null;
            /** @description The issuer's role AS IT IS NOW, not as it was at mint time. Null when the issuer is no longer a member. This is the role `effective_scopes` was derived from. */
            created_by_role?: components["schemas"]["MemberRole"] | null;
            /** Format: date-time */
            expires_at?: Record<string, never> | null;
            /**
             * Format: date-time
             * @description Best-effort, written by the data plane. Never a basis for an authorization call.
             */
            last_used_at?: Record<string, never> | null;
            /** Format: date-time */
            revoked_at?: Record<string, never> | null;
            /** Format: date-time */
            created_at: string;
            /**
             * @description THE PLAINTEXT KEY, RETURNED EXACTLY ONCE. It is not stored and cannot be recovered or redisplayed. Do not log it, do not put it in a URL, do not persist it anywhere but a secret store.
             * @example wk_live_3xAmPl3S3cr3tK3yV4lu3Chars32Aa
             */
            key: string;
        };
        EndpointDto: {
            id: string;
            project_id: string;
            name: string;
            url: string;
            description?: Record<string, never> | null;
            /** @enum {string} */
            status: "active" | "paused" | "disabled" | "deleted";
            /** @description Operator intent. The circuit breaker uses `status` instead. */
            enabled: boolean;
            /** @description Set by the circuit breaker. */
            disabled_reason?: Record<string, never> | null;
            disabled_at?: Record<string, never> | null;
            timeout_ms: number;
            max_concurrency: number;
            rate_limit?: Record<string, never> | null;
            rate_limit_window_seconds: number;
            retry_policy_id?: Record<string, never> | null;
            custom_headers?: {
                [key: string]: string;
            } | null;
            /**
             * @description Whether this endpoint has at least one signing secret that is signing RIGHT NOW - `active = true AND (expires_at IS NULL OR expires_at > now())`, the same pair the data plane's secret loader uses. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because `signing.Header` fails closed. Read `active` alone and the two answers disagree for the window between a secret expiring and the sweep flipping its column, which is exactly when an operator is looking.
             *
             *     A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only.
             * @example true
             */
            has_live_secret: boolean;
            created_at: string;
            updated_at: string;
        };
        EndpointListDto: {
            data: components["schemas"]["EndpointDto"][];
            /**
             * @description More endpoints match than this page carries.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass back as `offset` for the next page. NULL - never absent, never 0 - on the last one.
             * @example null
             */
            next_offset: number | null;
        };
        CreateEndpointDto: {
            /** @example Finance ledger */
            name: string;
            /**
             * @description http or https only. Credentials in the URL and literal private, loopback, link-local or cloud-metadata addresses are refused here so the failure is visible at save time; hostnames are re-checked against their RESOLVED address at dial time in the data plane, which is where DNS rebinding is actually stopped.
             * @example https://finance.example.com/webhooks/payments
             */
            url: string;
            description?: string;
            /**
             * @description How long one delivery attempt may hold a worker slot.
             * @default 30000
             */
            timeout_ms: number;
            /**
             * @description In-flight attempts allowed against this endpoint at once.
             * @default 16
             */
            max_concurrency: number;
            /** @description Deliveries per window. Null means no per-endpoint limit. */
            rate_limit?: Record<string, never> | null;
            /** @default 1 */
            rate_limit_window_seconds: number;
            /** @description Retry policy in THIS project. Resolved through the tenant scope. */
            retry_policy_id?: Record<string, never> | null;
            /** @description Up to 20 extra request headers. Webhook-*, Authorization, Host, Content-Length and Transfer-Encoding are reserved and rejected. */
            custom_headers?: {
                [key: string]: string;
            };
        };
        CreatedEndpointDto: {
            id: string;
            project_id: string;
            name: string;
            url: string;
            description?: Record<string, never> | null;
            /** @enum {string} */
            status: "active" | "paused" | "disabled" | "deleted";
            /** @description Operator intent. The circuit breaker uses `status` instead. */
            enabled: boolean;
            /** @description Set by the circuit breaker. */
            disabled_reason?: Record<string, never> | null;
            disabled_at?: Record<string, never> | null;
            timeout_ms: number;
            max_concurrency: number;
            rate_limit?: Record<string, never> | null;
            rate_limit_window_seconds: number;
            retry_policy_id?: Record<string, never> | null;
            custom_headers?: {
                [key: string]: string;
            } | null;
            /**
             * @description Whether this endpoint has at least one signing secret that is signing RIGHT NOW - `active = true AND (expires_at IS NULL OR expires_at > now())`, the same pair the data plane's secret loader uses. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because `signing.Header` fails closed. Read `active` alone and the two answers disagree for the window between a secret expiring and the sweep flipping its column, which is exactly when an operator is looking.
             *
             *     A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only.
             * @example true
             */
            has_live_secret: boolean;
            created_at: string;
            updated_at: string;
            /** @description The version 1 signing secret, in plaintext, returned HERE AND NOWHERE ELSE. Present only when the caller also holds `endpoint-secrets.write` (owner or admin): a developer may create endpoints but may not read signing secrets, so for them this is null, the endpoint stays paused, and `secret_pending` says so. */
            secret?: Record<string, never> | null;
            /** @description True when the endpoint was created with a signing secret this caller may not receive, so it is PAUSED and not delivering. An owner or admin must rotate (POST /v1/endpoints/{id}/secrets/rotate), hand the consumer the plaintext, then enable it. Going live here instead would sign every delivery with a key nobody holds: the consumer would reject all of them, and the rotation that fixed it would change the secret AGAIN - two verification outages instead of none. */
            secret_pending: boolean;
            /** @description Version of the secret that was minted with this endpoint. */
            secret_version: number;
        };
        UpdateEndpointDto: {
            /** @example Finance ledger */
            name?: string;
            /**
             * @description http or https only. Credentials in the URL and literal private, loopback, link-local or cloud-metadata addresses are refused here so the failure is visible at save time; hostnames are re-checked against their RESOLVED address at dial time in the data plane, which is where DNS rebinding is actually stopped.
             * @example https://finance.example.com/webhooks/payments
             */
            url?: string;
            description?: string;
            /**
             * @description How long one delivery attempt may hold a worker slot.
             * @default 30000
             */
            timeout_ms: number;
            /**
             * @description In-flight attempts allowed against this endpoint at once.
             * @default 16
             */
            max_concurrency: number;
            /** @description Deliveries per window. Null means no per-endpoint limit. */
            rate_limit?: Record<string, never> | null;
            /** @default 1 */
            rate_limit_window_seconds: number;
            /** @description Retry policy in THIS project. Resolved through the tenant scope. */
            retry_policy_id?: Record<string, never> | null;
            /** @description Up to 20 extra request headers. Webhook-*, Authorization, Host, Content-Length and Transfer-Encoding are reserved and rejected. */
            custom_headers?: {
                [key: string]: string;
            };
        };
        DisableEndpointDto: {
            /** @description Recorded in the audit log so the delivery gap can be explained later. */
            reason?: string;
        };
        EndpointSecretDto: {
            id: string;
            endpoint_id: string;
            /** @description Monotonic per endpoint. Highest version is the newest. */
            version: number;
            /** @description Whether this secret currently signs deliveries: the stored `active` flag AND an `expires_at` that has not passed. An expired row reads false here before any sweep has flipped the column. */
            active: boolean;
            /** @description When this secret stops signing. */
            expires_at?: Record<string, never> | null;
            /** @description When a rotation superseded it. */
            rotated_at?: Record<string, never> | null;
            created_at: string;
        };
        EndpointSecretListDto: {
            data: components["schemas"]["EndpointSecretDto"][];
            /**
             * @description More secrets match than this page carries.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass back as `offset` for the next page. NULL - never absent, never 0 - on the last one.
             * @example null
             */
            next_offset: number | null;
        };
        RotateSecretDto: {
            /**
             * @description How long the CURRENT secrets keep signing alongside the new one. Every active secret produces its own `v1=` component in Webhook-Signature and a consumer that matches any one of them verifies, so this is the window in which consumers can be rolled. 0 stops the old secrets immediately - use it for a leak, not for a routine rotation.
             * @default 86400
             */
            overlap_seconds: number;
        };
        RotatedSecretDto: {
            id: string;
            endpoint_id: string;
            /** @description Monotonic per endpoint. Highest version is the newest. */
            version: number;
            /** @description Whether this secret currently signs deliveries: the stored `active` flag AND an `expires_at` that has not passed. An expired row reads false here before any sweep has flipped the column. */
            active: boolean;
            /** @description When this secret stops signing. */
            expires_at?: Record<string, never> | null;
            /** @description When a rotation superseded it. */
            rotated_at?: Record<string, never> | null;
            created_at: string;
            /** @description The plaintext secret. RETURNED EXACTLY ONCE, in this response. It is encrypted at rest with a key this API does not hand out and is never included in any other response, log line or audit entry. If it is lost, rotate again. */
            secret: string;
            /** @description The LAST moment any previously issued secret still signs - the maximum over every version in `overlapping_versions`, not just the ones this rotation moved. Null when no prior secret is signing any more (a rotation with `overlap_seconds: 0`, or the first secret on an endpoint). Consumers must accept both old and new until this passes. */
            previous_secrets_expire_at?: Record<string, never> | null;
            /** @description EVERY prior version that still signs after this rotation, newest first - including versions whose own expiry this rotation did not move, because they are still emitting a `v1=` component and a consumer rolling its secrets off this list must know about them. */
            overlapping_versions: number[];
        };
        SubscriptionDto: {
            id: string;
            project_id: string;
            endpoint_id: string;
            name?: Record<string, never> | null;
            /** @description Exactly as stored. Never rewritten, never widened, never defaulted. */
            event_types: string[];
            /** @description The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. */
            payload_filter?: {
                [key: string]: unknown;
            } | null;
            /** @description A disabled subscription matches no events at all. */
            enabled: boolean;
            created_at: string;
            updated_at: string;
        };
        SubscriptionListDto: {
            data: components["schemas"]["SubscriptionDto"][];
            /** @description More subscriptions match than this page carries. */
            has_more: boolean;
            /** @description Pass back as `offset` for the next page. Null when this page was the last. */
            next_offset?: Record<string, never> | null;
        };
        CreateSubscriptionDto: {
            /**
             * @description For humans reading the subscription list. Optional; the column is nullable.
             * @example Finance ledger - settlements only
             */
            name?: Record<string, never> | null;
            /**
             * @description An endpoint in THIS project. Resolved through the tenant scope, so an id belonging to another customer answers 404 with the same message as an id that does not exist.
             * @example ep_01J8ZK...
             */
            endpoint_id: string;
            /**
             * @description The event types this subscription receives. Exactly three forms are accepted, and they are the three the router implements: "*" (everything), "payment.*" (every type beginning with the literal "payment.", so NOT "payments.settled" and NOT "payment"), and an exact type. Anything else is REFUSED - never widened to "*", never stored as something the router would read differently. An empty array is refused too: it would match no events at all. "*" may not appear alongside other patterns.
             * @example [
             *       "payment.settled",
             *       "payment.failed"
             *     ]
             */
            event_types: string[];
            /**
             * @description A JSON predicate applied to the event body after the event-type filter matches. Implicit AND over field paths; $and/$or/$not combine filters; $eq/$ne/$gt/$gte/$lt/$lte/$in/$nin/$exists compare one path. Strict JSON typing, no coercion; an absent path satisfies only {"$exists": false}. Bounded to 4096 bytes, 5 levels and 64 conditions. NOT YET EVALUATED BY THE DATA PLANE - a stored filter is inert until the router implements it. Send null, or omit it, for no body filter; an empty object is refused because it would match everything.
             * @example {
             *       "data.currency": "GHS",
             *       "data.amount": {
             *         "$gte": 1000
             *       }
             *     }
             */
            payload_filter?: {
                [key: string]: unknown;
            } | null;
            /**
             * @description A disabled subscription never matches - `Match()` skips it before the event-type test. This is how you stop deliveries without touching the filter.
             * @default true
             */
            enabled: boolean;
        };
        UpdateSubscriptionDto: {
            /** @description Null clears the name. */
            name?: Record<string, never> | null;
            /** @description Re-point this subscription at a different endpoint in the same project. Resolved through the tenant scope; a deleted endpoint is refused. */
            endpoint_id?: string;
            /** @description Replaces the whole filter - this is not a merge. The same three forms are accepted as on create, and the same refusals apply: an invalid pattern is a 400, never a silent widening to "*", and an empty array is refused. */
            event_types?: string[];
            /** @description Replaces the whole predicate. Null removes it. See the create DTO. */
            payload_filter?: {
                [key: string]: unknown;
            } | null;
        };
        DisableSubscriptionDto: {
            /** @description Recorded in the audit log so the delivery gap this creates can be explained later. */
            reason?: string;
        };
        RetryPolicyDto: {
            id: string;
            project_id: string;
            name: string;
            /** @description Exactly one policy per project carries this. */
            is_default: boolean;
            /** @enum {string} */
            strategy: "exponential" | "linear" | "constant";
            max_attempts: number;
            initial_delay_ms: number;
            max_delay_ms: number;
            multiplier: number;
            jitter_ratio: number;
            max_retry_duration_ms: number;
            created_at: string;
            updated_at: string;
        };
        RetryPolicyListDto: {
            data: components["schemas"]["RetryPolicyDto"][];
            /** @description More policies match than this page carries. The load-bearing field: a client cannot derive it from a total, and a total would cost a second COUNT on every list request. */
            has_more: boolean;
            /** @description `offset` for the next page, or null. */
            next_offset?: Record<string, never> | null;
        };
        CreateRetryPolicyDto: {
            /** @example Patient partners */
            name: string;
            /**
             * @description Make this the project default. Exactly one policy per project is the default; setting this clears the previous one in the same transaction. The FIRST policy created in a project becomes the default whether or not this is set, because a project with policies and no default is a state nothing can resolve.
             * @default false
             */
            is_default: boolean;
            /**
             * @description `exponential` multiplies the previous delay; `linear` adds `initial_delay_ms` each time; `constant` repeats `initial_delay_ms`. `multiplier` is read by `exponential` only — the delivery workers ignore it for the other two.
             * @default exponential
             * @enum {string}
             */
            strategy: "exponential" | "linear" | "constant";
            /**
             * @description Total attempts including the first delivery. Never 0: 0 means "no cap" downstream.
             * @default 8
             */
            max_attempts: number;
            /**
             * @description Delay before the FIRST retry. Must not exceed max_delay_ms.
             * @default 5000
             */
            initial_delay_ms: number;
            /**
             * @description Ceiling on any computed delay. MUST be positive: an unset ceiling is what let the exponential term overflow int64 in the delivery workers and schedule the next attempt permanently in the past.
             * @default 3600000
             */
            max_delay_ms: number;
            /**
             * @description Exponential growth factor. Must be strictly greater than 1 when strategy is `exponential` — the delivery workers replace any multiplier <= 1 with 2, so storing 1 would store a policy that does not describe what happens.
             * @default 2
             */
            multiplier: number;
            /**
             * @description Symmetric jitter as a fraction of the computed delay, so a thousand deliveries to one recovering endpoint do not stampede in lockstep. Above 1 the delay goes negative and is clamped to zero, which is the stampede again.
             * @default 0.2
             */
            jitter_ratio: number;
            /**
             * @description Wall-clock budget from the first attempt. Capped at 7 days because the column is a PostgreSQL integer. Never 0: 0 means "no budget cap" downstream, and eight attempts with an hour ceiling would keep a dead endpoint hot for days.
             * @default 86400000
             */
            max_retry_duration_ms: number;
        };
        UpdateRetryPolicyDto: {
            /** @example Patient partners */
            name?: string;
            /**
             * @description `exponential` multiplies the previous delay; `linear` adds `initial_delay_ms` each time; `constant` repeats `initial_delay_ms`. `multiplier` is read by `exponential` only — the delivery workers ignore it for the other two.
             * @default exponential
             * @enum {string}
             */
            strategy: "exponential" | "linear" | "constant";
            /**
             * @description Total attempts including the first delivery. Never 0: 0 means "no cap" downstream.
             * @default 8
             */
            max_attempts: number;
            /**
             * @description Delay before the FIRST retry. Must not exceed max_delay_ms.
             * @default 5000
             */
            initial_delay_ms: number;
            /**
             * @description Ceiling on any computed delay. MUST be positive: an unset ceiling is what let the exponential term overflow int64 in the delivery workers and schedule the next attempt permanently in the past.
             * @default 3600000
             */
            max_delay_ms: number;
            /**
             * @description Exponential growth factor. Must be strictly greater than 1 when strategy is `exponential` — the delivery workers replace any multiplier <= 1 with 2, so storing 1 would store a policy that does not describe what happens.
             * @default 2
             */
            multiplier: number;
            /**
             * @description Symmetric jitter as a fraction of the computed delay, so a thousand deliveries to one recovering endpoint do not stampede in lockstep. Above 1 the delay goes negative and is clamped to zero, which is the stampede again.
             * @default 0.2
             */
            jitter_ratio: number;
            /**
             * @description Wall-clock budget from the first attempt. Capped at 7 days because the column is a PostgreSQL integer. Never 0: 0 means "no budget cap" downstream, and eight attempts with an hour ceiling would keep a dead endpoint hot for days.
             * @default 86400000
             */
            max_retry_duration_ms: number;
        };
        RateLimitDto: {
            id: string;
            project_id: string;
            /** @enum {string} */
            scope: "organization" | "project" | "endpoint" | "ingest";
            /** @description Null means every resource in this scope. */
            resource_id?: Record<string, never> | null;
            limit: number;
            window_seconds: number;
            /** @description Null means the same as `limit`. */
            burst?: Record<string, never> | null;
            created_at: string;
            updated_at: string;
        };
        RateLimitListDto: {
            data: components["schemas"]["RateLimitDto"][];
            /** @description More policies match than this page carries. The load-bearing field: a client cannot derive it from a total, and a total would cost a second COUNT on every list request. */
            has_more: boolean;
            /** @description `offset` for the next page, or null. */
            next_offset?: Record<string, never> | null;
        };
        CreateRateLimitDto: {
            /**
             * @description What the ceiling applies to. `endpoint` and `project` bound outbound delivery; `organization` bounds it across every project in the org; `ingest` bounds inbound event acceptance.
             * @enum {string}
             */
            scope: "organization" | "project" | "endpoint" | "ingest";
            /** @description The specific resource this limit covers, or null for EVERY resource in this scope. What it names depends on `scope`: an endpoint id, an API key id (`ingest`), this project’s id, or this organization’s id. It is resolved through the scoped repository for that table, so an id belonging to another tenant is a 404. */
            resource_id?: Record<string, never> | null;
            /** @description Requests allowed per window. Never 0 or negative: 0 would disable delivery or ingestion entirely for whatever this policy covers. */
            limit: number;
            /**
             * @description Window length. Never 0: the refill rate is `limit / window`, so a zero window is a division by zero downstream.
             * @default 1
             */
            window_seconds: number;
            /** @description Bucket capacity. Null means "the same as `limit`". When set it must be at least `limit`, or the bucket could never hold one window’s worth of tokens and the configured limit would be unreachable. */
            burst?: Record<string, never> | null;
        };
        UpdateRateLimitDto: {
            /**
             * @description What the ceiling applies to. `endpoint` and `project` bound outbound delivery; `organization` bounds it across every project in the org; `ingest` bounds inbound event acceptance.
             * @enum {string}
             */
            scope?: "organization" | "project" | "endpoint" | "ingest";
            /** @description The specific resource this limit covers, or null for EVERY resource in this scope. What it names depends on `scope`: an endpoint id, an API key id (`ingest`), this project’s id, or this organization’s id. It is resolved through the scoped repository for that table, so an id belonging to another tenant is a 404. */
            resource_id?: Record<string, never> | null;
            /** @description Requests allowed per window. Never 0 or negative: 0 would disable delivery or ingestion entirely for whatever this policy covers. */
            limit?: number;
            /**
             * @description Window length. Never 0: the refill rate is `limit / window`, so a zero window is a division by zero downstream.
             * @default 1
             */
            window_seconds: number;
            /** @description Bucket capacity. Null means "the same as `limit`". When set it must be at least `limit`, or the bucket could never hold one window’s worth of tokens and the configured limit would be unreachable. */
            burst?: Record<string, never> | null;
        };
        EventDto: {
            id: string;
            project_id: string;
            event_type: string;
            /** @description The producer-supplied key that made ingest idempotent (ARCHITECTURE.md 17). */
            idempotency_key?: Record<string, never> | null;
            /** @description Opt-in serialisation key, carried onto every delivery this event fanned out to. Stored from day one; ordering enforcement is deferred (ADR-0004), so this does NOT currently guarantee anything about delivery order. */
            ordering_key?: Record<string, never> | null;
            /**
             * @description The INGEST/fan-out state, not a delivery outcome. `processed` means the fan-out committed, which says nothing about whether any endpoint accepted it - that is what the deliveries are for.
             * @enum {string}
             */
            status: "received" | "processing" | "processed" | "failed";
            /** @description Bytes of the authoritative payload as received. */
            payload_size: number;
            /** @description SHA-256 of the authoritative raw bytes, lowercase hex. */
            payload_hash: string;
            /** @description False when the raw bytes are not in the database (offloaded, or aged out). */
            payload_inline: boolean;
            /** @description `s3://bucket/key` when the payload was too large to store inline. */
            payload_location?: Record<string, never> | null;
            /** @description Ingest request headers. Credential-shaped values are `[redacted]`. */
            headers?: {
                [key: string]: string;
            } | null;
            created_at: string;
            /** @description When the fan-out first committed. Null until it has. */
            processed_at?: Record<string, never> | null;
        };
        EventListDto: {
            data: components["schemas"]["EventDto"][];
            has_more: boolean;
            next_offset?: Record<string, never> | null;
        };
        EventPayloadDto: {
            /**
             * @description Where the authoritative bytes are. `body` is non-null only for `inline`; the other two are told, not disguised as an empty payload.
             * @enum {string}
             */
            source: "inline" | "object_storage" | "unavailable";
            /** @description THE DELIVERED BYTES, decoded. This is what was signed. */
            body?: Record<string, never> | null;
            /**
             * @description `base64` when the payload is not valid UTF-8. Checked by re-encoding, so a binary body is never rendered as replacement characters that look like data.
             * @enum {string|null}
             */
            encoding?: "utf-8" | "base64" | null;
            location?: Record<string, never> | null;
            /** @description Bytes as received. Meaningful even when `body` is null. */
            size_bytes: number;
            /** @description SHA-256 of the authoritative bytes, lowercase hex. */
            sha256: string;
            /** @description THE JSONB COPY. For filtering, search and display convenience only. NOT what was delivered: PostgreSQL normalises jsonb, so key order, whitespace and duplicate keys differ from the bytes that were signed. Never verify a signature against this. */
            normalised_json?: {
                [key: string]: unknown;
            } | null;
            /** @description Says the above in a sentence, for whoever is reading the JSON. */
            notice: string;
        };
        EventDetailDto: {
            id: string;
            project_id: string;
            event_type: string;
            /** @description The producer-supplied key that made ingest idempotent (ARCHITECTURE.md 17). */
            idempotency_key?: Record<string, never> | null;
            /** @description Opt-in serialisation key, carried onto every delivery this event fanned out to. Stored from day one; ordering enforcement is deferred (ADR-0004), so this does NOT currently guarantee anything about delivery order. */
            ordering_key?: Record<string, never> | null;
            /**
             * @description The INGEST/fan-out state, not a delivery outcome. `processed` means the fan-out committed, which says nothing about whether any endpoint accepted it - that is what the deliveries are for.
             * @enum {string}
             */
            status: "received" | "processing" | "processed" | "failed";
            /** @description Bytes of the authoritative payload as received. */
            payload_size: number;
            /** @description SHA-256 of the authoritative raw bytes, lowercase hex. */
            payload_hash: string;
            /** @description False when the raw bytes are not in the database (offloaded, or aged out). */
            payload_inline: boolean;
            /** @description `s3://bucket/key` when the payload was too large to store inline. */
            payload_location?: Record<string, never> | null;
            /** @description Ingest request headers. Credential-shaped values are `[redacted]`. */
            headers?: {
                [key: string]: string;
            } | null;
            created_at: string;
            /** @description When the fan-out first committed. Null until it has. */
            processed_at?: Record<string, never> | null;
            payload: components["schemas"]["EventPayloadDto"];
        };
        DeliveryDto: {
            id: string;
            event_id: string;
            endpoint_id: string;
            /** @description The subscription that matched. Null on a replay whose subscription has since been deleted - the provenance of that replay is `replay_of_delivery_id`, which never goes away. */
            subscription_id?: Record<string, never> | null;
            project_id: string;
            /** @enum {string} */
            status: "pending" | "scheduled" | "queued" | "processing" | "succeeded" | "failed" | "retrying" | "exhausted" | "cancelled";
            /** @description Whether any further attempt will ever be made. */
            terminal: boolean;
            attempt_count: number;
            max_attempts: number;
            /** @description When the next attempt is due. Null means "as soon as a worker is free". */
            next_attempt_at?: Record<string, never> | null;
            last_attempt_at?: Record<string, never> | null;
            completed_at?: Record<string, never> | null;
            ordering_key?: Record<string, never> | null;
            /** @description The last failure, as the worker phrased it. The full history is in `attempts`. */
            last_error?: Record<string, never> | null;
            /** @description The worker holding this delivery, and until when. A row stuck in `processing` whose `locked_until` is in the past is a crashed worker; the scheduler reclaims it. */
            locked_by?: Record<string, never> | null;
            locked_until?: Record<string, never> | null;
            /** @description The delivery this one replays. Set on every replay and never on an original, which is what the partial unique index `deliveries_event_endpoint_original_key` relies on. */
            replay_of_delivery_id?: Record<string, never> | null;
            /** @description The user id that asked for the replay. Null on originals. */
            replayed_by?: Record<string, never> | null;
            /** @description Shorthand for `replay_of_delivery_id !== null`. */
            is_replay: boolean;
            created_at: string;
            updated_at: string;
        };
        DeliveryListDto: {
            data: components["schemas"]["DeliveryDto"][];
            has_more: boolean;
            next_offset?: Record<string, never> | null;
        };
        ReplayEventDto: {
            /** @description Recorded on the audit entry for this replay. Not stored on the delivery. */
            reason?: string;
            /** @description One of the endpoints this event was originally fanned out to. Omit to replay to all of them. An endpoint that never received this event is refused: sending it there for the first time is a new delivery, not a replay. */
            endpoint_id?: string;
        };
        ReplayResultDto: {
            /** @description The NEW delivery rows. Every one carries `replay_of_delivery_id`; no original was touched. */
            deliveries: components["schemas"]["DeliveryDto"][];
            replayed_count: number;
            /** @description The original delivery ids these replay, in the same order. */
            replay_of: string[];
        };
        DeliveryEventRefDto: {
            id: string;
            event_type: string;
            idempotency_key?: Record<string, never> | null;
            created_at: string;
        };
        DeliveryEndpointRefDto: {
            id: string;
            name: string;
            url: string;
            /**
             * @description The endpoint as it is NOW, not as it was when this delivery was made. A `deleted` or `disabled` endpoint here is why a replay of this delivery will be refused.
             * @enum {string}
             */
            status: "active" | "paused" | "disabled" | "deleted";
            disabled_reason?: Record<string, never> | null;
        };
        DeliveryAttemptDto: {
            id: string;
            delivery_id: string;
            /** @description 1-based, and unique per delivery. */
            attempt_number: number;
            /**
             * @description `failure` means the endpoint answered and we disliked the answer; `timeout`/`error` mean we never got one. The distinction is the first thing to look at.
             * @enum {string}
             */
            status: "success" | "failure" | "timeout" | "error";
            http_status?: Record<string, never> | null;
            started_at: string;
            completed_at?: Record<string, never> | null;
            duration_ms?: Record<string, never> | null;
            /** @description What we sent. Credential-shaped header VALUES are replaced with `[redacted]` - the key stays, so "did we send Authorization?" is still answerable. The signature header is NOT redacted: it is an HMAC over the payload, not the key, and it is what a consumer compares against when verification fails. */
            request_headers?: {
                [key: string]: string;
            } | null;
            response_headers?: {
                [key: string]: string;
            } | null;
            /** @description Truncated by the worker to EGRESS_MAX_RESPONSE_BYTES. */
            response_body?: Record<string, never> | null;
            /** @description Set instead of `response_body` when the response was too large to inline. */
            response_body_location?: Record<string, never> | null;
            /** @description Bytes the endpoint sent, BEFORE truncation. Compare with `response_body`. */
            response_size?: Record<string, never> | null;
            /** @description Low-cardinality classification derived from the error TYPE, never its message: `timeout`, `dns`, `connection`, `transport`, `blocked_target`, `signing_failed`, `payload_unavailable`, `permanent`, or `http_<status>`. */
            error_code?: Record<string, never> | null;
            error_message?: Record<string, never> | null;
            /** @description Which worker made this attempt. Useful when one replica is misbehaving. */
            worker_id?: Record<string, never> | null;
            created_at: string;
        };
        DeliveryDetailDto: {
            id: string;
            event_id: string;
            endpoint_id: string;
            /** @description The subscription that matched. Null on a replay whose subscription has since been deleted - the provenance of that replay is `replay_of_delivery_id`, which never goes away. */
            subscription_id?: Record<string, never> | null;
            project_id: string;
            /** @enum {string} */
            status: "pending" | "scheduled" | "queued" | "processing" | "succeeded" | "failed" | "retrying" | "exhausted" | "cancelled";
            /** @description Whether any further attempt will ever be made. */
            terminal: boolean;
            attempt_count: number;
            max_attempts: number;
            /** @description When the next attempt is due. Null means "as soon as a worker is free". */
            next_attempt_at?: Record<string, never> | null;
            last_attempt_at?: Record<string, never> | null;
            completed_at?: Record<string, never> | null;
            ordering_key?: Record<string, never> | null;
            /** @description The last failure, as the worker phrased it. The full history is in `attempts`. */
            last_error?: Record<string, never> | null;
            /** @description The worker holding this delivery, and until when. A row stuck in `processing` whose `locked_until` is in the past is a crashed worker; the scheduler reclaims it. */
            locked_by?: Record<string, never> | null;
            locked_until?: Record<string, never> | null;
            /** @description The delivery this one replays. Set on every replay and never on an original, which is what the partial unique index `deliveries_event_endpoint_original_key` relies on. */
            replay_of_delivery_id?: Record<string, never> | null;
            /** @description The user id that asked for the replay. Null on originals. */
            replayed_by?: Record<string, never> | null;
            /** @description Shorthand for `replay_of_delivery_id !== null`. */
            is_replay: boolean;
            created_at: string;
            updated_at: string;
            event: components["schemas"]["DeliveryEventRefDto"];
            endpoint: components["schemas"]["DeliveryEndpointRefDto"];
            /** @description Ascending by `attempt_number`. Append-only; nothing here is ever rewritten. */
            attempts: components["schemas"]["DeliveryAttemptDto"][];
            /** @description True when this delivery has more attempts than the inline cap. Page the rest from `GET /deliveries/:id/attempts`. */
            attempts_truncated: boolean;
        };
        DeliveryAttemptListDto: {
            data: components["schemas"]["DeliveryAttemptDto"][];
            has_more: boolean;
            next_offset?: Record<string, never> | null;
        };
        ReplayDeliveryDto: {
            /** @description Recorded on the audit entry for this replay. Not stored on the delivery. */
            reason?: string;
        };
        AuditLogDto: {
            /** @example aud_01J... */
            id: string;
            /** @example org_01J... */
            organization_id: string;
            /**
             * @description The user who did it. Null for a row with no user actor. Taken from the resolved tenant context at write time, never from a request body, so it cannot be forged.
             * @example usr_01J...
             */
            user_id: string | null;
            /**
             * @description Set instead of `user_id` when the actor was an API key. No path currently writes it - the column exists for the day an API-key principal carries a resolved context - so it reads back null on every row today.
             * @example null
             */
            api_key_id: string | null;
            /**
             * @description `<resource>.<verb>`, past tense. Not a closed enum; modules add their own.
             * @example endpoint.created
             */
            action: string;
            /** @example endpoint */
            resource_type: string;
            /**
             * @description The row that was acted on. Null when the action had no single subject.
             * @example ep_01J...
             */
            resource_id: string | null;
            /**
             * @description What changed, as recorded. ALREADY REDACTED at write time: values under key names that look like credentials read back as "[redacted]", and anything nested deeper than the walk goes reads back as "[truncated]". This route never reverses either. Null when the writer recorded nothing.
             * @example {
             *       "name": "finance",
             *       "url": "https://finance.example.com/hook"
             *     }
             */
            metadata: {
                [key: string]: unknown;
            } | null;
            /**
             * @description The address the action came from. This field, and `user_agent`, are why the permission is `audit.read` (owner/admin) and not `members.read`: these rows carry other members’ whereabouts, and viewer and billing hold `members.read`.
             * @example 203.0.113.9
             */
            ip_address: string | null;
            /** @example Mozilla/5.0 ... */
            user_agent: string | null;
            /** Format: date-time */
            created_at: string;
        };
        AuditLogListDto: {
            data: components["schemas"]["AuditLogDto"][];
            /**
             * @description True when more rows match this filter than the page carries. Read this, never a row count compared against `limit`, before concluding you have seen everything.
             * @example false
             */
            has_more: boolean;
            /**
             * @description Pass back as `offset` to fetch the next page. NULL - never absent, never 0 - when this page was the last one, so a client branches on one thing.
             * @example null
             */
            next_offset: number | null;
        };
        AnalyticsWindowDto: {
            /** @description Length of the window in hours, as requested. */
            hours: number;
            /**
             * Format: date-time
             * @description Inclusive lower bound.
             */
            from: string;
            /**
             * Format: date-time
             * @description EXCLUSIVE upper bound; "now" at query time.
             */
            to: string;
            /**
             * Format: date-time
             * @description Inclusive lower bound of the comparison window, which is the same length.
             */
            previous_from: string;
            /**
             * Format: date-time
             * @description Exclusive upper bound; equals `from`.
             */
            previous_to: string;
        };
        DeliveryStatusCountsDto: {
            pending: number;
            scheduled: number;
            queued: number;
            processing: number;
            succeeded: number;
            failed: number;
            retrying: number;
            exhausted: number;
            cancelled: number;
        };
        DeliveryOutcomeSummaryDto: {
            /** @description Every delivery created in the window, whatever its status. */
            total: number;
            succeeded: number;
            /** @description `failed` + `exhausted`: attempts that ended badly. `exhausted` is the subset that will never be retried again without a replay. */
            failing: number;
            /** @description Of `failing`, the ones that gave up: `exhausted`. */
            exhausted: number;
            /** @description Still moving: `pending`, `scheduled`, `queued`, `processing`, `retrying`. These are not failures yet and are excluded from `success_rate` for that reason. */
            in_flight: number;
            /** @description `cancelled`. Neither a success nor a failure. */
            cancelled: number;
            /** @description `succeeded / (succeeded + failed + exhausted)`, in 0..1, over deliveries that have SETTLED. NULL when nothing settled in the window - not 0, which means the opposite. */
            success_rate?: Record<string, never> | null;
            by_status: components["schemas"]["DeliveryStatusCountsDto"];
        };
        DeliveryOutcomesDto: {
            window: components["schemas"]["AnalyticsWindowDto"];
            current: components["schemas"]["DeliveryOutcomeSummaryDto"];
            /** @description The same numbers for the immediately preceding window of equal length. This is what makes "is it getting worse?" answerable from one request. */
            previous: components["schemas"]["DeliveryOutcomeSummaryDto"];
            /** @description `current.success_rate - previous.success_rate`. Negative means worse. NULL when either window had nothing settled, because a change from "unknown" is not a change. */
            success_rate_delta?: Record<string, never> | null;
            /** @description `current.total - previous.total`. Negative means quieter. */
            total_delta: number;
        };
        FailingEndpointDto: {
            endpoint_id: string;
            /** @description NULL when the endpoint row is gone. The delivery ledger is ON DELETE RESTRICT and endpoints are soft-deleted, so this should not happen - but the ranking is still the truth about what failed, and dropping the row to hide a missing name would hide the failure with it. */
            name?: Record<string, never> | null;
            url?: Record<string, never> | null;
            /** @description The endpoint status NOW - `active`, `paused`, `disabled`, `deleted`. */
            status?: Record<string, never> | null;
            /** @description Operator intent NOW. `false` with a `failing` count is an endpoint someone already turned off, or one the circuit breaker did. */
            enabled?: Record<string, never> | null;
            /** @description `failed` + `exhausted` in the window. The ranking key. */
            failing: number;
            failed: number;
            exhausted: number;
            /** @description In the window, and not yet a failure. */
            retrying: number;
            /** @description Every delivery to this endpoint in the window. */
            total: number;
            /** @description `failing / total`, in 0..1. Read it beside `failing`: one endpoint at 100% of two deliveries is not the outage; the one at 40% of twelve thousand is. */
            failure_rate: number;
        };
        FailingEndpointsDto: {
            window: components["schemas"]["AnalyticsWindowDto"];
            /** @description Worst first, by `failing`. */
            data: components["schemas"]["FailingEndpointDto"][];
            /** @description True when more endpoints had failures than `limit`. The ranking is still the worst ones; this says the list is not the whole set. */
            has_more: boolean;
        };
        AttemptLatencyDto: {
            window: components["schemas"]["AnalyticsWindowDto"];
            /** @description Nearest-rank p50 of `duration_ms`. */
            p50_ms?: Record<string, never> | null;
            /** @description Nearest-rank p95 of `duration_ms`. */
            p95_ms?: Record<string, never> | null;
            /** @description Nearest-rank p99 of `duration_ms`. */
            p99_ms?: Record<string, never> | null;
            min_ms?: Record<string, never> | null;
            max_ms?: Record<string, never> | null;
            /** @description How many attempts these percentiles were computed from. */
            sample_size: number;
            /** @description TRUE when the sample IS every measured attempt in the window, so the percentiles are exact. FALSE when the window held more traffic than the sample cap, in which case the numbers describe the MOST RECENT traffic in the window, not the whole of it. This flag is the honest part of the response - see HANDOFF.md for why an exact percentile is not reachable through the tenant scope today. */
            exact: boolean;
            /** @description How many deliveries the sampled attempts were drawn from. */
            sampled_deliveries: number;
        };
        EventTypeCountDto: {
            event_type: string;
            count: number;
        };
        EventVolumeDto: {
            window: components["schemas"]["AnalyticsWindowDto"];
            /** @description Events published in the window. */
            total: number;
            /** @description The same count for the preceding window of equal length. */
            previous_total: number;
            /** @description `total - previous_total`. */
            total_delta: number;
            /** @description Busiest first. */
            by_type: components["schemas"]["EventTypeCountDto"][];
            /** @description True when more event types occurred than `limit`. */
            has_more: boolean;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    AuthController_register: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RegisterDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AcknowledgedDto"];
                };
            };
        };
    };
    AuthController_login: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoginDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionResponseDto"];
                };
            };
        };
    };
    AuthController_logout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AcknowledgedDto"];
                };
            };
        };
    };
    AuthController_verifyEmail: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VerifyEmailDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionResponseDto"];
                };
            };
        };
    };
    AuthController_resendVerification: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResendVerificationDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AcknowledgedDto"];
                };
            };
        };
    };
    AuthController_forgotPassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ForgotPasswordDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AcknowledgedDto"];
                };
            };
        };
    };
    AuthController_resetPassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResetPasswordDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AcknowledgedDto"];
                };
            };
        };
    };
    AuthController_completeOnboarding: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Recorded, or already recorded. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AuthController_session: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionResponseDto"];
                };
            };
        };
    };
    OrganizationsController_list: {
        parameters: {
            query?: {
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrganizationListDto"];
                };
            };
        };
    };
    OrganizationsController_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateOrganizationDto"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrganizationDto"];
                };
            };
            /** @description Two different 409s, told apart by `error.code`, never by the message. `limit_exceeded` - the per-account organization cap is reached; `details` carries `{ limit, current, resource }`. `conflict` - the explicitly requested slug is already taken. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    OrganizationsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrganizationDto"];
                };
            };
            /** @description Absent, or the caller is not a member. Same answer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    OrganizationsController_remove: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The caller is a member but is not an owner. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    OrganizationsController_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateOrganizationDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrganizationDto"];
                };
            };
            /** @description The requested slug is already taken. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    MembersController_list: {
        parameters: {
            query?: {
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MemberListDto"];
                };
            };
            /** @description Absent, or the caller is not a member. Same answer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    MembersController_invite: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InviteMemberDto"];
            };
        };
        responses: {
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationAcceptedDto"];
                };
            };
            /** @description The caller may not assign that role. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    MembersController_remove: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                memberId: string;
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The lattice refused the removal. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The removal would leave the organization with no owner. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    MembersController_changeRole: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                memberId: string;
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateMemberRoleDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MemberDto"];
                };
            };
            /** @description The lattice refused the change. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The change would leave the organization with no owner. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    InvitationsController_accept: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AcceptInvitationDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AcceptedInvitationDto"];
                };
            };
            /** @description Unknown, consumed, expired, or issued to a different address - one answer for all four. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or the inviting member can no longer support the invitation. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ProjectsController_list: {
        parameters: {
            query?: {
                /** @description Defaults to everything except `deleted`. Pass `deleted` explicitly to see soft-deleted projects - they still hold their slug, so this is how you find out why a create 409d. */
                status?: components["schemas"]["ProjectStatus"];
                /** @description Page size. Clamped to 200 by the repository regardless. */
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProjectListDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or project is not visible to this caller - it does not exist, it is deleted, or it belongs to another tenant. All three answer identically and with the same message, so the response cannot be used to prove another customer's ids are real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ProjectsController_create: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateProjectDto"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProjectDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or project is not visible to this caller - it does not exist, it is deleted, or it belongs to another tenant. All three answer identically and with the same message, so the response cannot be used to prove another customer's ids are real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Two different 409s, told apart by `error.code`, never by the message. `limit_exceeded` - the organization is at its project ceiling; `details` carries `{ limit, current, resource }`. `conflict` - the slug is already taken in this organization. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ProjectsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                projectId: string;
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProjectDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or project is not visible to this caller - it does not exist, it is deleted, or it belongs to another tenant. All three answer identically and with the same message, so the response cannot be used to prove another customer's ids are real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ProjectsController_remove: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                projectId: string;
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProjectDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or project is not visible to this caller - it does not exist, it is deleted, or it belongs to another tenant. All three answer identically and with the same message, so the response cannot be used to prove another customer's ids are real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ProjectsController_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                projectId: string;
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateProjectDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProjectDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or project is not visible to this caller - it does not exist, it is deleted, or it belongs to another tenant. All three answer identically and with the same message, so the response cannot be used to prove another customer's ids are real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The slug is already taken in this organization. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ApiKeysController_list: {
        parameters: {
            query?: {
                /** @description Page size. Clamped to 200 by the repository regardless. */
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiKeyListDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or key is not visible to this caller - absent, deleted, or another tenant's. All of those answer identically, so this API cannot be used to confirm that an id is real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing is not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ApiKeysController_create: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateApiKeyDto"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CreatedApiKeyDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or key is not visible to this caller - absent, deleted, or another tenant's. All of those answer identically, so this API cannot be used to confirm that an id is real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `limit_exceeded` - the project is at its API-key ceiling; `details` carries `{ limit, current, resource }`. Revoke a key to free a slot. Match on `error.code`, never on the message. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing is not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    ApiKeysController_revoke: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                apiKeyId: string;
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiKeyDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or key is not visible to this caller - absent, deleted, or another tenant's. All of those answer identically, so this API cannot be used to confirm that an id is real. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing is not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_list: {
        parameters: {
            query?: {
                status?: "active" | "paused" | "disabled" | "deleted";
                /** @description Include soft-deleted endpoints. Deleted rows are kept forever because the delivery ledger references them; they are hidden from the default listing, not erased. */
                include_deleted?: boolean;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateEndpointDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CreatedEndpointDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description `limit_exceeded` - the project is at its endpoint ceiling; `details` carries `{ limit, current, resource }`. Every other 409 on this controller is a plain `conflict` (the endpoint is deleted, or has no active signing secret). Match on `error.code`, never on the message. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_remove: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateEndpointDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The endpoint has been deleted. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_enable: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Deleted, or no active signing secret - the latter is exactly `has_live_secret: false`. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointsController_disable: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DisableEndpointDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The endpoint has been deleted. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointSecretsController_list: {
        parameters: {
            query?: {
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointSecretListDto"];
                };
            };
            /** @description You are in this tenant, but signing secrets are owner/admin only. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such endpoint, or it belongs to another tenant - one answer for both. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointSecretsController_rotate: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RotateSecretDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RotatedSecretDto"];
                };
            };
            /** @description You are in this tenant, but signing secrets are owner/admin only. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such endpoint, or it belongs to another tenant - one answer for both. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The endpoint is deleted, or a concurrent rotation took the next version. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EndpointSecretsController_revoke: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                endpointId: string;
                secretId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EndpointSecretDto"];
                };
            };
            /** @description You are in this tenant, but signing secrets are owner/admin only. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description No such endpoint, or it belongs to another tenant - one answer for both. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description This is the endpoint`s last active secret. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_list: {
        parameters: {
            query?: {
                /** @description Only subscriptions bound to this endpoint. The id is resolved through the tenant scope first, so an endpoint belonging to another customer answers 404 rather than an empty list - the same answer an id that does not exist gets. */
                endpoint_id?: string;
                /** @description Filter by operator intent. Omit to see both enabled and disabled. */
                enabled?: boolean;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionListDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_create: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateSubscriptionDto"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionDto"];
                };
            };
            /** @description An event-type pattern is not one of "*", "prefix.*" or an exact type; `event_types` is empty; "*" appears alongside other patterns; or `payload_filter` is not a valid, bounded predicate. `details.field` names which. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Two different 409s, told apart by `error.code`, never by the message. `limit_exceeded` - the project is at its subscription ceiling; `details` carries `{ limit, current, resource }`. `conflict` - the endpoint named by `endpoint_id` has been deleted. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscriptionId: string;
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_remove: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscriptionId: string;
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscriptionId: string;
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateSubscriptionDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionDto"];
                };
            };
            /** @description Same validation as create. `details.field` names it. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The new endpoint has been deleted. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_enable: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscriptionId: string;
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    WebhookSubscriptionsController_disable: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscriptionId: string;
                projectId: unknown;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DisableSubscriptionDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionDto"];
                };
            };
            /** @description Membership is proven but the role is short of the permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RetryPoliciesController_list: {
        parameters: {
            query?: {
                /** @description Filter to the project default, or to everything else. */
                is_default?: boolean;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RetryPolicyListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RetryPoliciesController_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRetryPolicyDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RetryPolicyDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project is at its retry-policy ceiling. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RetryPoliciesController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RetryPolicyDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RetryPoliciesController_remove: {
        parameters: {
            query?: {
                /** @description The policy that becomes the project default. REQUIRED when deleting the current default while other policies remain; rejected otherwise, because it would silently change the default as a side effect of a delete that did not need to. */
                replacement_id?: string;
            };
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Endpoints still reference it, or it is the default and no successor was named. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RetryPoliciesController_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateRetryPolicyDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RetryPolicyDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RetryPoliciesController_setDefault: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RetryPolicyDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RateLimitsController_list: {
        parameters: {
            query?: {
                scope?: "organization" | "project" | "endpoint" | "ingest";
                /** @description Filter to the policies covering one specific resource. */
                resource_id?: string;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RateLimitListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, the policy, or the resource named by `resource_id` does not exist or belongs to another tenant. One answer, one message: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RateLimitsController_create: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRateLimitDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RateLimitDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, the policy, or the resource named by `resource_id` does not exist or belongs to another tenant. One answer, one message: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description A policy already covers this scope and resource, or the project is at its ceiling. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RateLimitsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RateLimitDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, the policy, or the resource named by `resource_id` does not exist or belongs to another tenant. One answer, one message: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RateLimitsController_remove: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, the policy, or the resource named by `resource_id` does not exist or belongs to another tenant. One answer, one message: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    RateLimitsController_update: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                policyId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateRateLimitDto"];
            };
        };
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RateLimitDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, the policy, or the resource named by `resource_id` does not exist or belongs to another tenant. One answer, one message: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Another policy already covers the new scope and resource. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EventsController_list: {
        parameters: {
            query?: {
                /** @description Exact event type, e.g. `payment.settled`. INDEX-SUPPORTED: leading columns of `events_project_id_event_type_created_at_idx`. No wildcards - a prefix search would silently stop using that index. */
                event_type?: string;
                /** @description Ingest/fan-out state, NOT a delivery outcome. **NOT INDEX-SUPPORTED**: a filter applied to whatever the project and date predicates selected. Pair it with a date range. */
                status?: "received" | "processing" | "processed" | "failed";
                /** @description Inclusive lower bound on `created_at`. INDEX-SUPPORTED. */
                created_after?: string;
                /** @description Exclusive upper bound on `created_at`. INDEX-SUPPORTED, and non-overlapping. */
                created_before?: string;
                /** @description Case-insensitive substring of the producer-supplied idempotency key - the free-text search for "the producer says they sent order 41f9, did we get it?". **NOT INDEX-SUPPORTED**: `ILIKE '%fragment%'` cannot use a b-tree, so this is a scan of every event the other filters left. Minimum 3 characters, and always pair it with a date range on a busy project. */
                idempotency_key?: string;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EventListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, event or endpoint does not exist, or belongs to another tenant. One answer with one message for all of them, on purpose: this route takes an event id in the path AND an endpoint id in the body, so distinguishable 404s would make it an oracle over another customer's endpoint ids. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EventsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                eventId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EventDetailDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, event or endpoint does not exist, or belongs to another tenant. One answer with one message for all of them, on purpose: this route takes an event id in the path AND an endpoint id in the body, so distinguishable 404s would make it an oracle over another customer's endpoint ids. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EventsController_deliveries: {
        parameters: {
            query?: {
                /** @description Exact status. INDEX-SUPPORTED: leading columns of `deliveries_project_id_status_created_at_idx`. */
                status?: "pending" | "scheduled" | "queued" | "processing" | "succeeded" | "failed" | "retrying" | "exhausted" | "cancelled";
                /** @description Everything that has failed and not recovered: `retrying`, `failed`, `exhausted`. INDEX-SUPPORTED (three scans of the same index). Cannot be combined with `status` - they would contradict each other and the API refuses rather than picking one. */
                failing_now?: boolean;
                /** @description INDEX-SUPPORTED: `deliveries_endpoint_id_created_at_idx`. */
                endpoint_id?: string;
                /** @description INDEX-SUPPORTED: `deliveries_event_id_idx`. */
                event_id?: string;
                /** @description The event type of the event this delivery came from. **NOT INDEX-SUPPORTED**: it is a join to `events` on a column `deliveries` does not carry, so it filters rows the project/status/date predicate already selected. Always combine it with a date range or an endpoint on a busy project. */
                event_type?: string;
                /** @description Inclusive lower bound on `created_at`. INDEX-SUPPORTED. */
                created_after?: string;
                /** @description Exclusive upper bound on `created_at`. INDEX-SUPPORTED. */
                created_before?: string;
                /** @description `original` hides replays, `replay` shows only them. Omitted, both are returned - a replay is a real delivery and hiding it by default would make the ledger lie. */
                origin?: "original" | "replay";
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                eventId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeliveryListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, event or endpoint does not exist, or belongs to another tenant. One answer with one message for all of them, on purpose: this route takes an event id in the path AND an endpoint id in the body, so distinguishable 404s would make it an oracle over another customer's endpoint ids. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    EventsController_replay: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                eventId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReplayEventDto"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReplayResultDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project, event or endpoint does not exist, or belongs to another tenant. One answer with one message for all of them, on purpose: this route takes an event id in the path AND an endpoint id in the body, so distinguishable 404s would make it an oracle over another customer's endpoint ids. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The event reached no endpoints, the named endpoint never received it, an endpoint is deleted or disabled, or the fan-out exceeds 50 deliveries. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    DeliveriesController_list: {
        parameters: {
            query?: {
                /** @description Exact status. INDEX-SUPPORTED: leading columns of `deliveries_project_id_status_created_at_idx`. */
                status?: "pending" | "scheduled" | "queued" | "processing" | "succeeded" | "failed" | "retrying" | "exhausted" | "cancelled";
                /** @description Everything that has failed and not recovered: `retrying`, `failed`, `exhausted`. INDEX-SUPPORTED (three scans of the same index). Cannot be combined with `status` - they would contradict each other and the API refuses rather than picking one. */
                failing_now?: boolean;
                /** @description INDEX-SUPPORTED: `deliveries_endpoint_id_created_at_idx`. */
                endpoint_id?: string;
                /** @description INDEX-SUPPORTED: `deliveries_event_id_idx`. */
                event_id?: string;
                /** @description The event type of the event this delivery came from. **NOT INDEX-SUPPORTED**: it is a join to `events` on a column `deliveries` does not carry, so it filters rows the project/status/date predicate already selected. Always combine it with a date range or an endpoint on a busy project. */
                event_type?: string;
                /** @description Inclusive lower bound on `created_at`. INDEX-SUPPORTED. */
                created_after?: string;
                /** @description Exclusive upper bound on `created_at`. INDEX-SUPPORTED. */
                created_before?: string;
                /** @description `original` hides replays, `replay` shows only them. Omitted, both are returned - a replay is a real delivery and hiding it by default would make the ledger lie. */
                origin?: "original" | "replay";
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeliveryListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or delivery does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else names a live delivery belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    DeliveriesController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                deliveryId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeliveryDetailDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or delivery does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else names a live delivery belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    DeliveriesController_attempts: {
        parameters: {
            query?: {
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                deliveryId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeliveryAttemptListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or delivery does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else names a live delivery belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    DeliveriesController_replay: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                deliveryId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReplayDeliveryDto"];
            };
        };
        responses: {
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReplayResultDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project or delivery does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else names a live delivery belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The endpoint is deleted or disabled, or the replay would exceed 50 deliveries. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AuditLogsController_list: {
        parameters: {
            query?: {
                /** @description Only rows attributed to this user. NOT INDEXED - a scan within the organization and date range. An id from another organization is not an error and not a leak: the tenant predicate is ANDed in, so it simply matches nothing. */
                user_id?: string;
                /** @description Exact match on the action, e.g. `api_key.revoked`. Index-supported. Actions are `<resource>.<verb>` in the past tense and are not a closed set, so an unknown value returns an empty page rather than a 400. */
                action?: string;
                /** @description e.g. `endpoint`, `api_key`, `member`. NOT INDEXED - a scan (see the class doc). */
                resource_type?: string;
                /** @description Everything that ever happened to one row - the "what happened to this endpoint?" question. NOT INDEXED - a scan (see the class doc); pair it with `created_after`. */
                resource_id?: string;
                /** @description Inclusive lower bound on `created_at`. Index-supported. */
                created_after?: string;
                /** @description Inclusive upper bound on `created_at`. Index-supported. */
                created_before?: string;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditLogListDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. `audit.read` is owner/admin: these rows carry other members’ actions, IP addresses and user agents. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or audit row does not exist, or belongs to another tenant. One answer with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else names a real row in someone else’s organization. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AuditLogsController_get: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                auditLogId: string;
                orgId: unknown;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditLogDto"];
                };
            };
            /** @description You are in this tenant but your role does not allow it. `audit.read` is owner/admin: these rows carry other members’ actions, IP addresses and user agents. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The organization or audit row does not exist, or belongs to another tenant. One answer with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else names a real row in someone else’s organization. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AnalyticsController_deliveries: {
        parameters: {
            query?: {
                /** @description How many hours back from now to summarise. The window is `[now - window_hours, now)`. Values above 720 (30 days) are REFUSED with a 400, never shortened: a clamped response would carry the number for a period the caller did not ask about. The dashboard shorthands map to 24 (24h), 168 (7d) and 720 (30d). */
                window_hours?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeliveryOutcomesDto"];
                };
            };
            /** @description `window_hours` was out of range - above 720 is REFUSED, never clamped. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project does not exist, or belongs to another tenant. One answer with one message, on purpose: a 403 here would confirm that a project id scraped from somewhere else names live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AnalyticsController_endpoints: {
        parameters: {
            query?: {
                /** @description How many hours back from now to summarise. The window is `[now - window_hours, now)`. Values above 720 (30 days) are REFUSED with a 400, never shortened: a clamped response would carry the number for a period the caller did not ask about. The dashboard shorthands map to 24 (24h), 168 (7d) and 720 (30d). */
                window_hours?: number;
                /** @description How many endpoints to return, worst first. Bounded because the second query groups by (endpoint_id, status) over these ids: the group count is `limit x 9` and a rollup that silently loses groups is worse than a smaller one. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FailingEndpointsDto"];
                };
            };
            /** @description `window_hours` was out of range - above 720 is REFUSED, never clamped. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project does not exist, or belongs to another tenant. One answer with one message, on purpose: a 403 here would confirm that a project id scraped from somewhere else names live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AnalyticsController_latency: {
        parameters: {
            query?: {
                /** @description How many hours back from now to summarise. The window is `[now - window_hours, now)`. Values above 720 (30 days) are REFUSED with a 400, never shortened: a clamped response would carry the number for a period the caller did not ask about. The dashboard shorthands map to 24 (24h), 168 (7d) and 720 (30d). */
                window_hours?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AttemptLatencyDto"];
                };
            };
            /** @description `window_hours` was out of range - above 720 is REFUSED, never clamped. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project does not exist, or belongs to another tenant. One answer with one message, on purpose: a 403 here would confirm that a project id scraped from somewhere else names live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    AnalyticsController_events: {
        parameters: {
            query?: {
                /** @description How many hours back from now to summarise. The window is `[now - window_hours, now)`. Values above 720 (30 days) are REFUSED with a 400, never shortened: a clamped response would carry the number for a period the caller did not ask about. The dashboard shorthands map to 24 (24h), 168 (7d) and 720 (30d). */
                window_hours?: number;
                /** @description How many event types to return, busiest first. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EventVolumeDto"];
                };
            };
            /** @description `window_hours` was out of range - above 720 is REFUSED, never clamped. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description You are in this tenant but your role does not allow it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description The project does not exist, or belongs to another tenant. One answer with one message, on purpose: a 403 here would confirm that a project id scraped from somewhere else names live infrastructure belonging to another customer. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
}
