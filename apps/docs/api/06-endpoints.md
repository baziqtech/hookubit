<!-- GENERATED by scripts/generate-api-reference.mjs from apps/control-api/openapi.json - do not edit -->

# Endpoints

> This page is generated from the same OpenAPI document the product is built from. If it disagrees with the product, the product wins - then tell us.

| Operation | Summary |
|---|---|
| [GET `/v1/projects/{projectId}/endpoints`](#get-v1-projects-projectid-endpoints) | List the endpoints in a project |
| [POST `/v1/projects/{projectId}/endpoints`](#post-v1-projects-projectid-endpoints) | Create an endpoint and its first signing secret |
| [GET `/v1/projects/{projectId}/endpoints/{endpointId}`](#get-v1-projects-projectid-endpoints-endpointid) | Fetch one endpoint |
| [PATCH `/v1/projects/{projectId}/endpoints/{endpointId}`](#patch-v1-projects-projectid-endpoints-endpointid) | Update an endpoint |
| [DELETE `/v1/projects/{projectId}/endpoints/{endpointId}`](#delete-v1-projects-projectid-endpoints-endpointid) | Remove an endpoint (soft delete) |
| [POST `/v1/projects/{projectId}/endpoints/{endpointId}/enable`](#post-v1-projects-projectid-endpoints-endpointid-enable) | Resume deliveries to an endpoint |
| [POST `/v1/projects/{projectId}/endpoints/{endpointId}/disable`](#post-v1-projects-projectid-endpoints-endpointid-disable) | Pause deliveries to an endpoint |

### GET `/v1/projects/{projectId}/endpoints`

**List the endpoints in a project**

Soft-deleted endpoints are hidden unless asked for; they are never erased. Paged with the canonical envelope `{ data, has_more, next_offset }`; `next_offset` is null on the last page.

Every row carries `has_live_secret`, answered for the whole page in one grouped read - so a UI can tell which paused endpoints can actually be resumed without asking per endpoint.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `status` | query | string | no | one of `active`, `paused`, `disabled`, `deleted` |  |
| `include_deleted` | query | boolean | no | default `false` | Include soft-deleted endpoints. Deleted rows are kept forever because the delivery ledger references them; they are hidden from the default listing, not erased. |
| `limit` | query | number | no | 1 to 200 |  |
| `offset` | query | number | no | min 0 |  |

#### Responses

**200 OK** - [EndpointListDto](./schemas.md#endpointlistdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `data` | [EndpointDto](./schemas.md#endpointdto)[] | yes |  |  |
| `data[].id` | string | yes |  |  |
| `data[].project_id` | string | yes |  |  |
| `data[].name` | string | yes |  |  |
| `data[].url` | string | yes |  |  |
| `data[].description` | string \| null | yes |  |  |
| `data[].status` | string | yes | one of `active`, `paused`, `disabled`, `deleted` |  |
| `data[].enabled` | boolean | yes |  | Operator intent. The circuit breaker uses `status` instead. |
| `data[].disabled_reason` | string \| null | yes |  | Why the PLATFORM disabled this endpoint, as a sentence, starting with `auto-disabled`. Set when the circuit breaker has been open past the configured window; cleared by `POST .../enable`. Null for an endpoint a human paused - that reason is in the audit log - so `status === "disabled" && disabled_reason !== null` is how the two are told apart. |
| `data[].disabled_at` | string \| null | yes |  | When the platform disabled it. Null unless `disabled_reason` is set. |
| `data[].timeout_ms` | number | yes |  |  |
| `data[].max_concurrency` | number | yes |  |  |
| `data[].rate_limit` | number \| null | yes |  |  |
| `data[].rate_limit_window_seconds` | number | yes |  |  |
| `data[].retry_policy_id` | string \| null | yes |  |  |
| `data[].custom_headers` | object<string, string> \| null | yes |  |  |
| `data[].has_live_secret` | boolean | yes |  | Whether this endpoint has at least one signing secret that is signing RIGHT NOW - one that is active and either has no expiry or has not reached it yet, which is the same test the delivery workers apply when they sign. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because signing fails closed rather than sending an unsigned request. Read a secret's `active` flag alone and the two answers disagree for the window between a secret expiring and its flag being swept, which is exactly when an operator is looking.<br><br>A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only. |
| `data[].created_at` | string | yes |  |  |
| `data[].updated_at` | string | yes |  |  |
| `data[].health` | [EndpointHealthDto](./schemas.md#endpointhealthdto) \| null | yes |  | How this endpoint has been doing, over a fixed trailing hour. Null when it could not be computed - never an invented zero. |
| `has_more` | boolean | yes |  | More endpoints match than this page carries. |
| `next_offset` | number \| null | yes |  | Pass back as `offset` for the next page. NULL - never absent, never 0 - on the last one. |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

### POST `/v1/projects/{projectId}/endpoints`

**Create an endpoint and its first signing secret**

The URL is checked for schemes, embedded credentials and literal private, loopback, link-local or cloud-metadata addresses so an unusable URL is refused now rather than becoming silent delivery failures later. A version 1 signing secret is minted with the endpoint - it is returned in this response only, and only to a caller who also holds `endpoint-secrets.write` (owner or admin).

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |

#### Request body

`application/json`, required - [CreateEndpointDto](./schemas.md#createendpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | yes | max 200 chars |  |
| `url` | string | yes | max 2048 chars | http or https only. Credentials in the URL and literal private, loopback, link-local or cloud-metadata addresses are refused here so the failure is visible at save time; hostnames are re-checked against their RESOLVED address at dial time in the data plane, which is where DNS rebinding is actually stopped. |
| `description` | string | no | max 1000 chars |  |
| `timeout_ms` | number | no | 1000 to 120000; default `30000` | How long one delivery attempt may hold a worker slot. |
| `max_concurrency` | number | no | 1 to 256; default `16` | In-flight attempts allowed against this endpoint at once. |
| `rate_limit` | number \| null | no | 1 to 100000 | Deliveries per window. Null means no per-endpoint limit. |
| `rate_limit_window_seconds` | number | no | 1 to 3600; default `1` |  |
| `retry_policy_id` | string \| null | no | max 64 chars | Retry policy in THIS project. Resolved through the tenant scope. |
| `custom_headers` | object<string, string> | no |  | Up to 20 extra request headers. Webhook-*, Authorization, Host, Content-Length and Transfer-Encoding are reserved and rejected. |

#### Responses

**200 OK** - [CreatedEndpointDto](./schemas.md#createdendpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `url` | string | yes |  |  |
| `description` | string \| null | yes |  |  |
| `status` | string | yes | one of `active`, `paused`, `disabled`, `deleted` |  |
| `enabled` | boolean | yes |  | Operator intent. The circuit breaker uses `status` instead. |
| `disabled_reason` | string \| null | yes |  | Why the PLATFORM disabled this endpoint, as a sentence, starting with `auto-disabled`. Set when the circuit breaker has been open past the configured window; cleared by `POST .../enable`. Null for an endpoint a human paused - that reason is in the audit log - so `status === "disabled" && disabled_reason !== null` is how the two are told apart. |
| `disabled_at` | string \| null | yes |  | When the platform disabled it. Null unless `disabled_reason` is set. |
| `timeout_ms` | number | yes |  |  |
| `max_concurrency` | number | yes |  |  |
| `rate_limit` | number \| null | yes |  |  |
| `rate_limit_window_seconds` | number | yes |  |  |
| `retry_policy_id` | string \| null | yes |  |  |
| `custom_headers` | object<string, string> \| null | yes |  |  |
| `has_live_secret` | boolean | yes |  | Whether this endpoint has at least one signing secret that is signing RIGHT NOW - one that is active and either has no expiry or has not reached it yet, which is the same test the delivery workers apply when they sign. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because signing fails closed rather than sending an unsigned request. Read a secret's `active` flag alone and the two answers disagree for the window between a secret expiring and its flag being swept, which is exactly when an operator is looking.<br><br>A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |
| `health` | [EndpointHealthDto](./schemas.md#endpointhealthdto) \| null | yes |  | How this endpoint has been doing, over a fixed trailing hour. Null when it could not be computed - never an invented zero. |
| `health.success_rate_1h` | number \| null | yes |  | Successes over SETTLED deliveries in the last hour. NULL, never 0, when nothing settled — an endpoint with no traffic reads "no data", and 0 means every delivery we attempted failed. Rendering the null as 0% turns a new endpoint into an outage. |
| `health.deliveries_1h` | number | yes |  | Deliveries created in the last hour. Context for the rate. |
| `health.deliveries_waiting` | number | yes |  | Created and still moving, at ANY age — not hour-bounded, because "what is queued behind this problem?" is not a question about the last hour. |
| `health.consecutive_failures` | number | yes |  | From the circuit breaker. Zero when it has not been failing. |
| `health.opened_at` | string \| null | yes |  | When the breaker opened. Null when it is not open. |
| `health.last_delivery_at` | string \| null | yes |  |  |
| `secret` | string \| null | yes |  | The version 1 signing secret, in plaintext, returned HERE AND NOWHERE ELSE. Present only when the caller also holds `endpoint-secrets.write` (owner or admin): a developer may create endpoints but may not read signing secrets, so for them this is null, the endpoint stays paused, and `secret_pending` says so. |
| `secret_pending` | boolean | yes |  | True when the endpoint was created with a signing secret this caller may not receive, so it is PAUSED and not delivering. An owner or admin must rotate (POST /v1/endpoints/{id}/secrets/rotate), hand the consumer the plaintext, then enable it. Going live here instead would sign every delivery with a key nobody holds: the consumer would reject all of them, and the rotation that fixed it would change the secret AGAIN - two verification outages instead of none. |
| `secret_version` | number | yes |  | Version of the secret that was minted with this endpoint. |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

`limit_exceeded` - the project is at its endpoint ceiling; `details` carries `{ limit, current, resource }`. Every other 409 on this controller is a plain `conflict` (the endpoint is deleted, or has no active signing secret). Match on `error.code`, never on the message.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### GET `/v1/projects/{projectId}/endpoints/{endpointId}`

**Fetch one endpoint**

Returns soft-deleted endpoints too, with `status: "deleted"`, so a delivery in the ledger that points at a removed endpoint is still readable. `has_live_secret` is the same answer the listing gives for this endpoint.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `endpointId` | path | string | yes |  |  |

#### Responses

**200 OK** - [EndpointDto](./schemas.md#endpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `url` | string | yes |  |  |
| `description` | string \| null | yes |  |  |
| `status` | string | yes | one of `active`, `paused`, `disabled`, `deleted` |  |
| `enabled` | boolean | yes |  | Operator intent. The circuit breaker uses `status` instead. |
| `disabled_reason` | string \| null | yes |  | Why the PLATFORM disabled this endpoint, as a sentence, starting with `auto-disabled`. Set when the circuit breaker has been open past the configured window; cleared by `POST .../enable`. Null for an endpoint a human paused - that reason is in the audit log - so `status === "disabled" && disabled_reason !== null` is how the two are told apart. |
| `disabled_at` | string \| null | yes |  | When the platform disabled it. Null unless `disabled_reason` is set. |
| `timeout_ms` | number | yes |  |  |
| `max_concurrency` | number | yes |  |  |
| `rate_limit` | number \| null | yes |  |  |
| `rate_limit_window_seconds` | number | yes |  |  |
| `retry_policy_id` | string \| null | yes |  |  |
| `custom_headers` | object<string, string> \| null | yes |  |  |
| `has_live_secret` | boolean | yes |  | Whether this endpoint has at least one signing secret that is signing RIGHT NOW - one that is active and either has no expiry or has not reached it yet, which is the same test the delivery workers apply when they sign. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because signing fails closed rather than sending an unsigned request. Read a secret's `active` flag alone and the two answers disagree for the window between a secret expiring and its flag being swept, which is exactly when an operator is looking.<br><br>A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |
| `health` | [EndpointHealthDto](./schemas.md#endpointhealthdto) \| null | yes |  | How this endpoint has been doing, over a fixed trailing hour. Null when it could not be computed - never an invented zero. |
| `health.success_rate_1h` | number \| null | yes |  | Successes over SETTLED deliveries in the last hour. NULL, never 0, when nothing settled — an endpoint with no traffic reads "no data", and 0 means every delivery we attempted failed. Rendering the null as 0% turns a new endpoint into an outage. |
| `health.deliveries_1h` | number | yes |  | Deliveries created in the last hour. Context for the rate. |
| `health.deliveries_waiting` | number | yes |  | Created and still moving, at ANY age — not hour-bounded, because "what is queued behind this problem?" is not a question about the last hour. |
| `health.consecutive_failures` | number | yes |  | From the circuit breaker. Zero when it has not been failing. |
| `health.opened_at` | string \| null | yes |  | When the breaker opened. Null when it is not open. |
| `health.last_delivery_at` | string \| null | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

### PATCH `/v1/projects/{projectId}/endpoints/{endpointId}`

**Update an endpoint**

`status` is not writable here. Enabling, disabling and deleting have their own routes because each has a precondition a PATCH would walk past.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `endpointId` | path | string | yes |  |  |

#### Request body

`application/json`, required - [UpdateEndpointDto](./schemas.md#updateendpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | max 200 chars |  |
| `url` | string | no | max 2048 chars | http or https only. Credentials in the URL and literal private, loopback, link-local or cloud-metadata addresses are refused here so the failure is visible at save time; hostnames are re-checked against their RESOLVED address at dial time in the data plane, which is where DNS rebinding is actually stopped. |
| `description` | string | no | max 1000 chars |  |
| `timeout_ms` | number | no | 1000 to 120000; default `30000` | How long one delivery attempt may hold a worker slot. |
| `max_concurrency` | number | no | 1 to 256; default `16` | In-flight attempts allowed against this endpoint at once. |
| `rate_limit` | number \| null | no | 1 to 100000 | Deliveries per window. Null means no per-endpoint limit. |
| `rate_limit_window_seconds` | number | no | 1 to 3600; default `1` |  |
| `retry_policy_id` | string \| null | no | max 64 chars | Retry policy in THIS project. Resolved through the tenant scope. |
| `custom_headers` | object<string, string> | no |  | Up to 20 extra request headers. Webhook-*, Authorization, Host, Content-Length and Transfer-Encoding are reserved and rejected. |

#### Responses

**200 OK** - [EndpointDto](./schemas.md#endpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `url` | string | yes |  |  |
| `description` | string \| null | yes |  |  |
| `status` | string | yes | one of `active`, `paused`, `disabled`, `deleted` |  |
| `enabled` | boolean | yes |  | Operator intent. The circuit breaker uses `status` instead. |
| `disabled_reason` | string \| null | yes |  | Why the PLATFORM disabled this endpoint, as a sentence, starting with `auto-disabled`. Set when the circuit breaker has been open past the configured window; cleared by `POST .../enable`. Null for an endpoint a human paused - that reason is in the audit log - so `status === "disabled" && disabled_reason !== null` is how the two are told apart. |
| `disabled_at` | string \| null | yes |  | When the platform disabled it. Null unless `disabled_reason` is set. |
| `timeout_ms` | number | yes |  |  |
| `max_concurrency` | number | yes |  |  |
| `rate_limit` | number \| null | yes |  |  |
| `rate_limit_window_seconds` | number | yes |  |  |
| `retry_policy_id` | string \| null | yes |  |  |
| `custom_headers` | object<string, string> \| null | yes |  |  |
| `has_live_secret` | boolean | yes |  | Whether this endpoint has at least one signing secret that is signing RIGHT NOW - one that is active and either has no expiry or has not reached it yet, which is the same test the delivery workers apply when they sign. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because signing fails closed rather than sending an unsigned request. Read a secret's `active` flag alone and the two answers disagree for the window between a secret expiring and its flag being swept, which is exactly when an operator is looking.<br><br>A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |
| `health` | [EndpointHealthDto](./schemas.md#endpointhealthdto) \| null | yes |  | How this endpoint has been doing, over a fixed trailing hour. Null when it could not be computed - never an invented zero. |
| `health.success_rate_1h` | number \| null | yes |  | Successes over SETTLED deliveries in the last hour. NULL, never 0, when nothing settled — an endpoint with no traffic reads "no data", and 0 means every delivery we attempted failed. Rendering the null as 0% turns a new endpoint into an outage. |
| `health.deliveries_1h` | number | yes |  | Deliveries created in the last hour. Context for the rate. |
| `health.deliveries_waiting` | number | yes |  | Created and still moving, at ANY age — not hour-bounded, because "what is queued behind this problem?" is not a question about the last hour. |
| `health.consecutive_failures` | number | yes |  | From the circuit breaker. Zero when it has not been failing. |
| `health.opened_at` | string \| null | yes |  | When the breaker opened. Null when it is not open. |
| `health.last_delivery_at` | string \| null | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The endpoint has been deleted.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### DELETE `/v1/projects/{projectId}/endpoints/{endpointId}`

**Remove an endpoint (soft delete)**

Sets `status` to `deleted`. The row is kept forever: `deliveries.endpoint_id` is ON DELETE RESTRICT precisely so that removing an endpoint cannot erase the record of what it was sent. Idempotent.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `endpointId` | path | string | yes |  |  |

#### Responses

**204 No Content** - no body

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

### POST `/v1/projects/{projectId}/endpoints/{endpointId}/enable`

**Resume deliveries to an endpoint**

Refused when the endpoint has no active signing secret: the data plane fails closed rather than delivering unsigned, so enabling would only queue failures.

This is also the way back from an automatic disable. It clears `disabled_reason` and `disabled_at`, and brings the circuit breaker`s next probe forward to now - so recovery starts immediately instead of waiting out a cooldown that has doubled to its ceiling, while still admitting exactly ONE delivery until the endpoint answers. A backlog is never released at an endpoint whose recovery has not been observed yet.

**Check `has_live_secret` before offering this.** It is the same condition, evaluated the same way, and it is false on a normal, expected state - an endpoint created by a `developer` stays paused with `secret_pending` because `endpoint-secrets.*` is owner/admin only. Offering the action there is offering a guaranteed 409; the operator wants "rotate a secret first", not a refusal after the click.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `endpointId` | path | string | yes |  |  |

#### Responses

**200 OK** - [EndpointDto](./schemas.md#endpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `url` | string | yes |  |  |
| `description` | string \| null | yes |  |  |
| `status` | string | yes | one of `active`, `paused`, `disabled`, `deleted` |  |
| `enabled` | boolean | yes |  | Operator intent. The circuit breaker uses `status` instead. |
| `disabled_reason` | string \| null | yes |  | Why the PLATFORM disabled this endpoint, as a sentence, starting with `auto-disabled`. Set when the circuit breaker has been open past the configured window; cleared by `POST .../enable`. Null for an endpoint a human paused - that reason is in the audit log - so `status === "disabled" && disabled_reason !== null` is how the two are told apart. |
| `disabled_at` | string \| null | yes |  | When the platform disabled it. Null unless `disabled_reason` is set. |
| `timeout_ms` | number | yes |  |  |
| `max_concurrency` | number | yes |  |  |
| `rate_limit` | number \| null | yes |  |  |
| `rate_limit_window_seconds` | number | yes |  |  |
| `retry_policy_id` | string \| null | yes |  |  |
| `custom_headers` | object<string, string> \| null | yes |  |  |
| `has_live_secret` | boolean | yes |  | Whether this endpoint has at least one signing secret that is signing RIGHT NOW - one that is active and either has no expiry or has not reached it yet, which is the same test the delivery workers apply when they sign. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because signing fails closed rather than sending an unsigned request. Read a secret's `active` flag alone and the two answers disagree for the window between a secret expiring and its flag being swept, which is exactly when an operator is looking.<br><br>A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |
| `health` | [EndpointHealthDto](./schemas.md#endpointhealthdto) \| null | yes |  | How this endpoint has been doing, over a fixed trailing hour. Null when it could not be computed - never an invented zero. |
| `health.success_rate_1h` | number \| null | yes |  | Successes over SETTLED deliveries in the last hour. NULL, never 0, when nothing settled — an endpoint with no traffic reads "no data", and 0 means every delivery we attempted failed. Rendering the null as 0% turns a new endpoint into an outage. |
| `health.deliveries_1h` | number | yes |  | Deliveries created in the last hour. Context for the rate. |
| `health.deliveries_waiting` | number | yes |  | Created and still moving, at ANY age — not hour-bounded, because "what is queued behind this problem?" is not a question about the last hour. |
| `health.consecutive_failures` | number | yes |  | From the circuit breaker. Zero when it has not been failing. |
| `health.opened_at` | string \| null | yes |  | When the breaker opened. Null when it is not open. |
| `health.last_delivery_at` | string \| null | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Deleted, or no active signing secret - the latter is exactly `has_live_secret: false`.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### POST `/v1/projects/{projectId}/endpoints/{endpointId}/disable`

**Pause deliveries to an endpoint**

Sets `status` to `paused`. `disabled_reason` and `disabled_at` are left untouched - they are the record of an AUTOMATIC disable, and overwriting them here would erase why the platform stopped delivering. The reason given here goes to the audit log.

Note what the data plane does with a paused endpoint: a delivery already queued for it is finished `cancelled` ("we stopped on purpose"), not retried and not failed, and new events stop producing delivery rows for it. Nothing already recorded in the ledger is discarded.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `endpointId` | path | string | yes |  |  |

#### Request body

`application/json`, required - [DisableEndpointDto](./schemas.md#disableendpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `reason` | string | no | max 200 chars | Recorded in the audit log so the delivery gap can be explained later. |

#### Responses

**200 OK** - [EndpointDto](./schemas.md#endpointdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `url` | string | yes |  |  |
| `description` | string \| null | yes |  |  |
| `status` | string | yes | one of `active`, `paused`, `disabled`, `deleted` |  |
| `enabled` | boolean | yes |  | Operator intent. The circuit breaker uses `status` instead. |
| `disabled_reason` | string \| null | yes |  | Why the PLATFORM disabled this endpoint, as a sentence, starting with `auto-disabled`. Set when the circuit breaker has been open past the configured window; cleared by `POST .../enable`. Null for an endpoint a human paused - that reason is in the audit log - so `status === "disabled" && disabled_reason !== null` is how the two are told apart. |
| `disabled_at` | string \| null | yes |  | When the platform disabled it. Null unless `disabled_reason` is set. |
| `timeout_ms` | number | yes |  |  |
| `max_concurrency` | number | yes |  |  |
| `rate_limit` | number \| null | yes |  |  |
| `rate_limit_window_seconds` | number | yes |  |  |
| `retry_policy_id` | string \| null | yes |  |  |
| `custom_headers` | object<string, string> \| null | yes |  |  |
| `has_live_secret` | boolean | yes |  | Whether this endpoint has at least one signing secret that is signing RIGHT NOW - one that is active and either has no expiry or has not reached it yet, which is the same test the delivery workers apply when they sign. False means `POST /enable` will refuse with 409: an enabled endpoint with nothing to sign with delivers nothing, because signing fails closed rather than sending an unsigned request. Read a secret's `active` flag alone and the two answers disagree for the window between a secret expiring and its flag being swept, which is exactly when an operator is looking.<br><br>A BOOLEAN, deliberately: no id, version, prefix or expiry. This field is visible to anyone with `endpoints.read` (a viewer included), and reading signing secrets is `endpoint-secrets.read` - owner and admin only. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |
| `health` | [EndpointHealthDto](./schemas.md#endpointhealthdto) \| null | yes |  | How this endpoint has been doing, over a fixed trailing hour. Null when it could not be computed - never an invented zero. |
| `health.success_rate_1h` | number \| null | yes |  | Successes over SETTLED deliveries in the last hour. NULL, never 0, when nothing settled — an endpoint with no traffic reads "no data", and 0 means every delivery we attempted failed. Rendering the null as 0% turns a new endpoint into an outage. |
| `health.deliveries_1h` | number | yes |  | Deliveries created in the last hour. Context for the rate. |
| `health.deliveries_waiting` | number | yes |  | Created and still moving, at ANY age — not hour-bounded, because "what is queued behind this problem?" is not a question about the last hour. |
| `health.consecutive_failures` | number | yes |  | From the circuit breaker. Zero when it has not been failing. |
| `health.opened_at` | string \| null | yes |  | When the breaker opened. Null when it is not open. |
| `health.last_delivery_at` | string \| null | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or endpoint does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The endpoint has been deleted.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)
