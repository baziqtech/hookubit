<!-- GENERATED by scripts/generate-api-reference.mjs from apps/control-api/openapi.json - do not edit -->

# Retry policies

> This page is generated from the same OpenAPI document the product is built from. If it disagrees with the product, the product wins - then tell us.

| Operation | Summary |
|---|---|
| [GET `/v1/projects/{projectId}/retry-policies`](#get-v1-projects-projectid-retry-policies) | List the retry policies in a project |
| [POST `/v1/projects/{projectId}/retry-policies`](#post-v1-projects-projectid-retry-policies) | Create a retry policy |
| [GET `/v1/projects/{projectId}/retry-policies/{policyId}`](#get-v1-projects-projectid-retry-policies-policyid) | Fetch one retry policy |
| [PATCH `/v1/projects/{projectId}/retry-policies/{policyId}`](#patch-v1-projects-projectid-retry-policies-policyid) | Update a retry policy |
| [DELETE `/v1/projects/{projectId}/retry-policies/{policyId}`](#delete-v1-projects-projectid-retry-policies-policyid) | Delete a retry policy |
| [POST `/v1/projects/{projectId}/retry-policies/{policyId}/default`](#post-v1-projects-projectid-retry-policies-policyid-default) | Make this the project default retry policy |

### GET `/v1/projects/{projectId}/retry-policies`

**List the retry policies in a project**

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `is_default` | query | boolean | no |  | Filter to the project default, or to everything else. |
| `limit` | query | number | no | 1 to 200 |  |
| `offset` | query | number | no | min 0 |  |

#### Responses

**200 OK** - [RetryPolicyListDto](./schemas.md#retrypolicylistdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `data` | [RetryPolicyDto](./schemas.md#retrypolicydto)[] | yes |  |  |
| `data[].id` | string | yes |  |  |
| `data[].project_id` | string | yes |  |  |
| `data[].name` | string | yes |  |  |
| `data[].is_default` | boolean | yes |  | Exactly one policy per project carries this. |
| `data[].strategy` | string | yes | one of `exponential`, `linear`, `constant` |  |
| `data[].max_attempts` | number | yes |  |  |
| `data[].initial_delay_ms` | number | yes |  |  |
| `data[].max_delay_ms` | number | yes |  |  |
| `data[].multiplier` | number | yes |  |  |
| `data[].jitter_ratio` | number | yes |  |  |
| `data[].max_retry_duration_ms` | number | yes |  |  |
| `data[].created_at` | string | yes |  |  |
| `data[].updated_at` | string | yes |  |  |
| `has_more` | boolean | yes |  | More policies match than this page carries. The load-bearing field: a client cannot derive it from a total, and a total would cost a second COUNT on every list request. |
| `next_offset` | number \| null | yes |  | `offset` for the next page, or null. |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

### POST `/v1/projects/{projectId}/retry-policies`

**Create a retry policy**

Every field is bounded to something the delivery workers can compute a positive, finite delay from — an unbounded `max_delay_ms` is what once overflowed the computed duration and scheduled retries permanently in the past. The FIRST policy in a project becomes its default automatically.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |

#### Request body

`application/json`, required - [CreateRetryPolicyDto](./schemas.md#createretrypolicydto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | yes | max 200 chars |  |
| `is_default` | boolean | no | default `false` | Make this the project default. Exactly one policy per project is the default; setting this clears the previous one in the same transaction. The FIRST policy created in a project becomes the default whether or not this is set, because a project with policies and no default is a state nothing can resolve. |
| `strategy` | string | no | one of `exponential`, `linear`, `constant`; default `"exponential"` | `exponential` multiplies the previous delay; `linear` adds `initial_delay_ms` each time; `constant` repeats `initial_delay_ms`. `multiplier` is read by `exponential` only — the delivery workers ignore it for the other two. |
| `max_attempts` | number | no | 1 to 50; default `8` | Total attempts including the first delivery. Never 0: 0 means "no cap" downstream. |
| `initial_delay_ms` | number | no | 1 to 86400000; default `5000` | Delay before the FIRST retry. Must not exceed max_delay_ms. |
| `max_delay_ms` | number | no | 1 to 86400000; default `3600000` | Ceiling on any computed delay. MUST be positive: an unset ceiling is what let the exponential term overflow int64 in the delivery workers and schedule the next attempt permanently in the past. |
| `multiplier` | number | no | 1 to 100; default `2` | Exponential growth factor. Must be strictly greater than 1 when strategy is `exponential` — the delivery workers replace any multiplier &lt;= 1 with 2, so storing 1 would store a policy that does not describe what happens. |
| `jitter_ratio` | number | no | 0 to 1; default `0.2` | Symmetric jitter as a fraction of the computed delay, so a thousand deliveries to one recovering endpoint do not stampede in lockstep. Above 1 the delay goes negative and is clamped to zero, which is the stampede again. |
| `max_retry_duration_ms` | number | no | 1000 to 604800000; default `86400000` | Wall-clock budget from the first attempt. Capped at 7 days because the column is a PostgreSQL integer. Never 0: 0 means "no budget cap" downstream, and eight attempts with an hour ceiling would keep a dead endpoint hot for days. |

#### Responses

**200 OK** - [RetryPolicyDto](./schemas.md#retrypolicydto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `is_default` | boolean | yes |  | Exactly one policy per project carries this. |
| `strategy` | string | yes | one of `exponential`, `linear`, `constant` |  |
| `max_attempts` | number | yes |  |  |
| `initial_delay_ms` | number | yes |  |  |
| `max_delay_ms` | number | yes |  |  |
| `multiplier` | number | yes |  |  |
| `jitter_ratio` | number | yes |  |  |
| `max_retry_duration_ms` | number | yes |  |  |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project is at its retry-policy ceiling.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### GET `/v1/projects/{projectId}/retry-policies/{policyId}`

**Fetch one retry policy**

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `policyId` | path | string | yes |  |  |

#### Responses

**200 OK** - [RetryPolicyDto](./schemas.md#retrypolicydto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `is_default` | boolean | yes |  | Exactly one policy per project carries this. |
| `strategy` | string | yes | one of `exponential`, `linear`, `constant` |  |
| `max_attempts` | number | yes |  |  |
| `initial_delay_ms` | number | yes |  |  |
| `max_delay_ms` | number | yes |  |  |
| `multiplier` | number | yes |  |  |
| `jitter_ratio` | number | yes |  |  |
| `max_retry_duration_ms` | number | yes |  |  |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

### PATCH `/v1/projects/{projectId}/retry-policies/{policyId}`

**Update a retry policy**

`is_default` is not writable here — it has its own route, because "exactly one default per project" is a property of a set of rows and needs a transaction that clears the old one. The patch is validated MERGED with the stored row, so a single field cannot walk the policy into an incoherent combination.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `policyId` | path | string | yes |  |  |

#### Request body

`application/json`, required - [UpdateRetryPolicyDto](./schemas.md#updateretrypolicydto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | max 200 chars |  |
| `strategy` | string | no | one of `exponential`, `linear`, `constant`; default `"exponential"` | `exponential` multiplies the previous delay; `linear` adds `initial_delay_ms` each time; `constant` repeats `initial_delay_ms`. `multiplier` is read by `exponential` only — the delivery workers ignore it for the other two. |
| `max_attempts` | number | no | 1 to 50; default `8` | Total attempts including the first delivery. Never 0: 0 means "no cap" downstream. |
| `initial_delay_ms` | number | no | 1 to 86400000; default `5000` | Delay before the FIRST retry. Must not exceed max_delay_ms. |
| `max_delay_ms` | number | no | 1 to 86400000; default `3600000` | Ceiling on any computed delay. MUST be positive: an unset ceiling is what let the exponential term overflow int64 in the delivery workers and schedule the next attempt permanently in the past. |
| `multiplier` | number | no | 1 to 100; default `2` | Exponential growth factor. Must be strictly greater than 1 when strategy is `exponential` — the delivery workers replace any multiplier &lt;= 1 with 2, so storing 1 would store a policy that does not describe what happens. |
| `jitter_ratio` | number | no | 0 to 1; default `0.2` | Symmetric jitter as a fraction of the computed delay, so a thousand deliveries to one recovering endpoint do not stampede in lockstep. Above 1 the delay goes negative and is clamped to zero, which is the stampede again. |
| `max_retry_duration_ms` | number | no | 1000 to 604800000; default `86400000` | Wall-clock budget from the first attempt. Capped at 7 days because the column is a PostgreSQL integer. Never 0: 0 means "no budget cap" downstream, and eight attempts with an hour ceiling would keep a dead endpoint hot for days. |

#### Responses

**200 OK** - [RetryPolicyDto](./schemas.md#retrypolicydto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `is_default` | boolean | yes |  | Exactly one policy per project carries this. |
| `strategy` | string | yes | one of `exponential`, `linear`, `constant` |  |
| `max_attempts` | number | yes |  |  |
| `initial_delay_ms` | number | yes |  |  |
| `max_delay_ms` | number | yes |  |  |
| `multiplier` | number | yes |  |  |
| `jitter_ratio` | number | yes |  |  |
| `max_retry_duration_ms` | number | yes |  |  |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

### DELETE `/v1/projects/{projectId}/retry-policies/{policyId}`

**Delete a retry policy**

Refused while any live endpoint references it: `endpoints.retry_policy_id` is ON DELETE SET NULL, so the delete would silently move those endpoints onto the platform default backoff. Deleting the project default also requires `?replacement_id=`, unless it is the only policy left.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `policyId` | path | string | yes |  |  |
| `replacement_id` | query | string | no | max 64 chars | The policy that becomes the project default. REQUIRED when deleting the current default while other policies remain; rejected otherwise, because it would silently change the default as a side effect of a delete that did not need to. |

#### Responses

**204 No Content** - no body

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Endpoints still reference it, or it is the default and no successor was named.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### POST `/v1/projects/{projectId}/retry-policies/{policyId}/default`

**Make this the project default retry policy**

Clears the previous default and sets this one inside a single SERIALIZABLE transaction. Two concurrent calls cannot both win: the schema has no partial unique index, so nothing else would stop a project ending up with two defaults.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. Resolved from the project row, never trusted as a claim. |
| `policyId` | path | string | yes |  |  |

#### Responses

**200 OK** - [RetryPolicyDto](./schemas.md#retrypolicydto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `name` | string | yes |  |  |
| `is_default` | boolean | yes |  | Exactly one policy per project carries this. |
| `strategy` | string | yes | one of `exponential`, `linear`, `constant` |  |
| `max_attempts` | number | yes |  |  |
| `initial_delay_ms` | number | yes |  |  |
| `max_delay_ms` | number | yes |  |  |
| `multiplier` | number | yes |  |  |
| `jitter_ratio` | number | yes |  |  |
| `max_retry_duration_ms` | number | yes |  |  |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or policy does not exist, or belongs to another tenant. The two are one answer, with one message, on purpose: a 403 here would confirm that an id scraped from somewhere else is live configuration belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)
