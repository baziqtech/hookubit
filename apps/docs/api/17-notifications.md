<!-- GENERATED by scripts/generate-api-reference.mjs from apps/control-api/openapi.json - do not edit -->

# Notifications

> This page is generated from the same OpenAPI document the product is built from. If it disagrees with the product, the product wins - then tell us.

| Operation | Summary |
|---|---|
| [GET `/v1/projects/{projectId}/notification-destinations`](#get-v1-projects-projectid-notification-destinations) | Where this project sends alerts |
| [POST `/v1/projects/{projectId}/notification-destinations`](#post-v1-projects-projectid-notification-destinations) | Add a destination and send its confirmation |
| [PATCH `/v1/projects/{projectId}/notification-destinations/{destinationId}`](#patch-v1-projects-projectid-notification-destinations-destinationid) | Rename a destination, or change what it receives |
| [DELETE `/v1/projects/{projectId}/notification-destinations/{destinationId}`](#delete-v1-projects-projectid-notification-destinations-destinationid) | Remove a destination |
| [POST `/v1/projects/{projectId}/notification-destinations/{destinationId}/resend`](#post-v1-projects-projectid-notification-destinations-destinationid-resend) | Send the confirmation again, with a NEW token |
| [POST `/v1/projects/{projectId}/notification-destinations/{destinationId}/test`](#post-v1-projects-projectid-notification-destinations-destinationid-test) | Send a test alert |
| [POST `/v1/notification-destinations/confirm`](#post-v1-notification-destinations-confirm) | Confirm an address for alerts |

### GET `/v1/projects/{projectId}/notification-destinations`

**Where this project sends alerts**

Readable by everyone who can read the delivery record: "who gets told when this breaks?" is part of understanding what happened.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  |  |

#### Responses

**200 OK** - [DestinationListDto](./schemas.md#destinationlistdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `data` | [DestinationDto](./schemas.md#destinationdto)[] | yes |  |  |
| `data[].id` | string | yes |  |  |
| `data[].project_id` | string | yes |  |  |
| `data[].kind` | string | yes | one of `email`, `slack` |  |
| `data[].target` | string | yes |  | The address. Lower-cased on the way in. |
| `data[].label` | string | yes |  |  |
| `data[].status` | string | yes | one of `pending`, `confirmed`, `failing`, `disabled` | `pending` receives NOTHING. A destination is silent until somebody who can read the address clicks the confirmation link — which is also the only check available that the address is real, so a typo sits here visibly rather than swallowing every alert. |
| `data[].events` | string[] | yes |  | Which triggers this destination has asked for. An EMPTY list receives nothing and is a legal, deliberate state: it is how a destination is muted without deleting it and losing its confirmation. |
| `data[].confirmed_at` | string \| null | yes |  |  |
| `data[].last_sent_at` | string \| null | yes |  |  |
| `data[].last_error` | string \| null | yes |  | Why the last attempt to reach this destination failed, if one did. |
| `data[].created_at` | string | yes |  |  |
| `has_more` | boolean | yes |  |  |
| `next_offset` | number \| null | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or the destination does not exist or belongs to another tenant. One answer, one message.

`error.code`: [`not_found`](./errors.md#not-found)

### POST `/v1/projects/{projectId}/notification-destinations`

**Add a destination and send its confirmation**

The destination is created `pending` and receives NOTHING until somebody who can read the address clicks the link. The row is written before the message is sent and a send failure does not roll it back: a pending destination with a Resend button is a better place to be than a form you have to fill in again.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  |  |

#### Request body

`application/json`, required - [CreateDestinationDto](./schemas.md#createdestinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `kind` | string | yes | one of `email` | Only 'email' today. A Slack destination needs an app installed in your workspace; accepting the value before that exists would create a row nothing can deliver to. |
| `target` | string | yes |  |  |
| `label` | string | no |  | What to call it on screen. Defaults to the address. |
| `events` | string[] | no |  | Omitted, the destination is subscribed to everything. |

#### Responses

**200 OK** - [DestinationDto](./schemas.md#destinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `kind` | string | yes | one of `email`, `slack` |  |
| `target` | string | yes |  | The address. Lower-cased on the way in. |
| `label` | string | yes |  |  |
| `status` | string | yes | one of `pending`, `confirmed`, `failing`, `disabled` | `pending` receives NOTHING. A destination is silent until somebody who can read the address clicks the confirmation link — which is also the only check available that the address is real, so a typo sits here visibly rather than swallowing every alert. |
| `events` | string[] | yes |  | Which triggers this destination has asked for. An EMPTY list receives nothing and is a legal, deliberate state: it is how a destination is muted without deleting it and losing its confirmation. |
| `confirmed_at` | string \| null | yes |  |  |
| `last_sent_at` | string \| null | yes |  |  |
| `last_error` | string \| null | yes |  | Why the last attempt to reach this destination failed, if one did. |
| `created_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or the destination does not exist or belongs to another tenant. One answer, one message.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

This address is already a destination for this project, or the project is at its ceiling.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### PATCH `/v1/projects/{projectId}/notification-destinations/{destinationId}`

**Rename a destination, or change what it receives**

`events` REPLACES the list. An empty array mutes the destination without deleting it, which keeps its confirmation.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  |  |
| `destinationId` | path | string | yes |  |  |

#### Request body

`application/json`, required - [UpdateDestinationDto](./schemas.md#updatedestinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `label` | string | no |  |  |
| `events` | string[] | no |  | REPLACES the list. An empty array mutes the destination without deleting it. |

#### Responses

**200 OK** - [DestinationDto](./schemas.md#destinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `kind` | string | yes | one of `email`, `slack` |  |
| `target` | string | yes |  | The address. Lower-cased on the way in. |
| `label` | string | yes |  |  |
| `status` | string | yes | one of `pending`, `confirmed`, `failing`, `disabled` | `pending` receives NOTHING. A destination is silent until somebody who can read the address clicks the confirmation link — which is also the only check available that the address is real, so a typo sits here visibly rather than swallowing every alert. |
| `events` | string[] | yes |  | Which triggers this destination has asked for. An EMPTY list receives nothing and is a legal, deliberate state: it is how a destination is muted without deleting it and losing its confirmation. |
| `confirmed_at` | string \| null | yes |  |  |
| `last_sent_at` | string \| null | yes |  |  |
| `last_error` | string \| null | yes |  | Why the last attempt to reach this destination failed, if one did. |
| `created_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or the destination does not exist or belongs to another tenant. One answer, one message.

`error.code`: [`not_found`](./errors.md#not-found)

### DELETE `/v1/projects/{projectId}/notification-destinations/{destinationId}`

**Remove a destination**

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  |  |
| `destinationId` | path | string | yes |  |  |

#### Responses

**204 No Content** - no body

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or the destination does not exist or belongs to another tenant. One answer, one message.

`error.code`: [`not_found`](./errors.md#not-found)

### POST `/v1/projects/{projectId}/notification-destinations/{destinationId}/resend`

**Send the confirmation again, with a NEW token**

New rather than re-sent: the old link may be sitting in a mailbox somebody no longer has access to, which is very often exactly why a resend is being asked for.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  |  |
| `destinationId` | path | string | yes |  |  |

#### Responses

**200 OK** - [DestinationDto](./schemas.md#destinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `kind` | string | yes | one of `email`, `slack` |  |
| `target` | string | yes |  | The address. Lower-cased on the way in. |
| `label` | string | yes |  |  |
| `status` | string | yes | one of `pending`, `confirmed`, `failing`, `disabled` | `pending` receives NOTHING. A destination is silent until somebody who can read the address clicks the confirmation link — which is also the only check available that the address is real, so a typo sits here visibly rather than swallowing every alert. |
| `events` | string[] | yes |  | Which triggers this destination has asked for. An EMPTY list receives nothing and is a legal, deliberate state: it is how a destination is muted without deleting it and losing its confirmation. |
| `confirmed_at` | string \| null | yes |  |  |
| `last_sent_at` | string \| null | yes |  |  |
| `last_error` | string \| null | yes |  | Why the last attempt to reach this destination failed, if one did. |
| `created_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or the destination does not exist or belongs to another tenant. One answer, one message.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Already confirmed; there is nothing to send.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

### POST `/v1/projects/{projectId}/notification-destinations/{destinationId}/test`

**Send a test alert**

Goes through the same template and the same transport a real alert takes, so an arrival proves the path. It is NOT recorded as a dispatch, so it neither satisfies nor triggers the grouping rule — sending a test must not make the next real alert disappear.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  |  |
| `destinationId` | path | string | yes |  |  |

#### Responses

**204 No Content** - no body

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

You are in this tenant but your role does not allow it.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project or the destination does not exist or belongs to another tenant. One answer, one message.

`error.code`: [`not_found`](./errors.md#not-found)

### POST `/v1/notification-destinations/confirm`

**Confirm an address for alerts**

Single use. "No such token", "already used" and "expired" all answer the same 404 with the same message: this endpoint is reachable by anyone, so distinguishing them is an oracle for which tokens exist — and all three are fixed the same way.

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [ConfirmDestinationDto](./schemas.md#confirmdestinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `token` | string | yes |  | The token from the confirmation link. |

#### Responses

**200 OK** - [ConfirmedDestinationDto](./schemas.md#confirmeddestinationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `project_name` | string | yes |  |  |
| `target` | string | yes |  | The address that was confirmed, so the page can name it. |
