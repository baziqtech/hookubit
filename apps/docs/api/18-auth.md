<!-- GENERATED by scripts/generate-api-reference.mjs from apps/control-api/openapi.json - do not edit -->

# Authentication and sessions

> This page is generated from the same OpenAPI document the product is built from. If it disagrees with the product, the product wins - then tell us.

| Operation | Summary |
|---|---|
| [POST `/v1/auth/register`](#post-v1-auth-register) | Create an account and its organization |
| [POST `/v1/auth/login`](#post-v1-auth-login) | Exchange credentials for a session cookie |
| [POST `/v1/auth/logout`](#post-v1-auth-logout) | Clear the session cookie |
| [POST `/v1/auth/verify-email`](#post-v1-auth-verify-email) | Consume a single-use email verification token |
| [POST `/v1/auth/resend-verification`](#post-v1-auth-resend-verification) | Request a fresh email verification link |
| [POST `/v1/auth/forgot-password`](#post-v1-auth-forgot-password) | Request a password reset link |
| [POST `/v1/auth/reset-password`](#post-v1-auth-reset-password) | Consume a single-use reset token and set a new password |
| [POST `/v1/auth/onboarding-completed`](#post-v1-auth-onboarding-completed) | Mark the product tour as finished or skipped |
| [GET `/v1/auth/session`](#get-v1-auth-session) | Return the user behind the current session cookie |

### POST `/v1/auth/register`

**Create an account and its organization**

Refused with `forbidden` unless ALLOW_OPEN_REGISTRATION=true. Otherwise always 202, whether or not the address is already registered - a 201/409 split let an attacker enumerate accounts. No session cookie is set: the user verifies the address from the email and then logs in. A taken address gets a "someone tried to register" notice instead.

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [RegisterDto](./schemas.md#registerdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `email` | string | yes | max 320 chars |  |
| `password` | string | yes | 12 to 128 chars | Never logged, never returned. |
| `name` | string | no | max 200 chars |  |
| `organization_name` | string | no | max 200 chars | Organization created for this user. Defaults to the local part of the email. |

#### Responses

**202 Accepted** - [AcknowledgedDto](./schemas.md#acknowledgeddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `status` | string | yes |  | Deliberately uninformative: password reset and verification responses must not reveal whether an account exists. |

### POST `/v1/auth/login`

**Exchange credentials for a session cookie**

Returns `unauthenticated` for unknown, disabled and wrong-password alike. A correct password on an account whose address was never confirmed returns `email_not_verified` instead - only reachable once the password is already known, so it enumerates nothing.

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [LoginDto](./schemas.md#logindto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `email` | string | yes |  |  |
| `password` | string | yes |  | Never logged, never returned. |

#### Responses

**200 OK** - [SessionResponseDto](./schemas.md#sessionresponsedto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `user` | [AuthUserDto](./schemas.md#authuserdto) | yes |  |  |
| `user.id` | string | yes |  |  |
| `user.email` | string | yes |  |  |
| `user.name` | string \| null | yes |  |  |
| `user.email_verified` | boolean | yes |  | False until the verification token is presented. |
| `user.onboarding_completed_at` | string \| null | yes | format `date-time` | ISO-8601 instant at which the user finished OR skipped the product tour; null if neither. Carried on every response that returns a user - session, login, verify-email - so the client never needs a second request to decide whether to show the tour. Set by POST /v1/auth/onboarding-completed. |

### POST `/v1/auth/logout`

**Clear the session cookie**

Idempotent.

**Auth:** signed-in dashboard session (cookie).

#### Responses

**200 OK** - [AcknowledgedDto](./schemas.md#acknowledgeddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `status` | string | yes |  | Deliberately uninformative: password reset and verification responses must not reveal whether an account exists. |

### POST `/v1/auth/verify-email`

**Consume a single-use email verification token**

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [VerifyEmailDto](./schemas.md#verifyemaildto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `token` | string | yes |  | Single-use token from the verification email. |

#### Responses

**200 OK** - [SessionResponseDto](./schemas.md#sessionresponsedto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `user` | [AuthUserDto](./schemas.md#authuserdto) | yes |  |  |
| `user.id` | string | yes |  |  |
| `user.email` | string | yes |  |  |
| `user.name` | string \| null | yes |  |  |
| `user.email_verified` | boolean | yes |  | False until the verification token is presented. |
| `user.onboarding_completed_at` | string \| null | yes | format `date-time` | ISO-8601 instant at which the user finished OR skipped the product tour; null if neither. Carried on every response that returns a user - session, login, verify-email - so the client never needs a second request to decide whether to show the tour. Set by POST /v1/auth/onboarding-completed. |

### POST `/v1/auth/resend-verification`

**Request a fresh email verification link**

ALWAYS 202, with an identical body: whether or not the address is registered, whether or not it is already verified, whether or not the account is disabled, and whether or not the mail transport is up. A degraded mailer must not turn this into an oracle - the same trap that was found and fixed in forgot-password. Any previous verification link is invalidated, so the newest email is the one that works.

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [ResendVerificationDto](./schemas.md#resendverificationdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `email` | string | yes |  |  |

#### Responses

**202 Accepted** - [AcknowledgedDto](./schemas.md#acknowledgeddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `status` | string | yes |  | Deliberately uninformative: password reset and verification responses must not reveal whether an account exists. |

### POST `/v1/auth/forgot-password`

**Request a password reset link**

Always 202, whether or not the address is registered.

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [ForgotPasswordDto](./schemas.md#forgotpassworddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `email` | string | yes |  |  |

#### Responses

**202 Accepted** - [AcknowledgedDto](./schemas.md#acknowledgeddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `status` | string | yes |  | Deliberately uninformative: password reset and verification responses must not reveal whether an account exists. |

### POST `/v1/auth/reset-password`

**Consume a single-use reset token and set a new password**

Clears any existing session; the user must log in again.

**Auth:** none - this route is public.

#### Request body

`application/json`, required - [ResetPasswordDto](./schemas.md#resetpassworddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `token` | string | yes |  | Single-use token from the password reset email. |
| `password` | string | yes | 12 to 128 chars |  |

#### Responses

**200 OK** - [AcknowledgedDto](./schemas.md#acknowledgeddto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `status` | string | yes |  | Deliberately uninformative: password reset and verification responses must not reveal whether an account exists. |

### POST `/v1/auth/onboarding-completed`

**Mark the product tour as finished or skipped**

Idempotent: completing twice is a 204, not a conflict, and the recorded instant is the FIRST completion - it never drifts forward on a replay. Acts only on the calling user's own row; there is no body and no path parameter, so one account cannot complete another's. Read the resulting instant back from `user.onboarding_completed_at` on GET /v1/auth/session.

**Auth:** signed-in dashboard session (cookie).

#### Responses

**204 No Content** - no body

Recorded, or already recorded.

### GET `/v1/auth/session`

**Return the user behind the current session cookie**

**Auth:** signed-in dashboard session (cookie).

#### Responses

**200 OK** - [SessionResponseDto](./schemas.md#sessionresponsedto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `user` | [AuthUserDto](./schemas.md#authuserdto) | yes |  |  |
| `user.id` | string | yes |  |  |
| `user.email` | string | yes |  |  |
| `user.name` | string \| null | yes |  |  |
| `user.email_verified` | boolean | yes |  | False until the verification token is presented. |
| `user.onboarding_completed_at` | string \| null | yes | format `date-time` | ISO-8601 instant at which the user finished OR skipped the product tour; null if neither. Carried on every response that returns a user - session, login, verify-email - so the client never needs a second request to decide whether to show the tour. Set by POST /v1/auth/onboarding-completed. |
