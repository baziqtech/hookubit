<!-- GENERATED by scripts/generate-api-reference.mjs from apps/control-api/openapi.json - do not edit -->

# Subscriptions

> This page is generated from the same OpenAPI document the product is built from. If it disagrees with the product, the product wins - then tell us.

| Operation | Summary |
|---|---|
| [GET `/v1/projects/{projectId}/subscriptions`](#get-v1-projects-projectid-subscriptions) | List the subscriptions in a project |
| [POST `/v1/projects/{projectId}/subscriptions`](#post-v1-projects-projectid-subscriptions) | Subscribe an endpoint to a set of event types |
| [GET `/v1/projects/{projectId}/subscriptions/{subscriptionId}`](#get-v1-projects-projectid-subscriptions-subscriptionid) | Fetch one subscription |
| [PATCH `/v1/projects/{projectId}/subscriptions/{subscriptionId}`](#patch-v1-projects-projectid-subscriptions-subscriptionid) | Update a subscription |
| [DELETE `/v1/projects/{projectId}/subscriptions/{subscriptionId}`](#delete-v1-projects-projectid-subscriptions-subscriptionid) | Remove a subscription (hard delete) |
| [POST `/v1/projects/{projectId}/subscriptions/{subscriptionId}/enable`](#post-v1-projects-projectid-subscriptions-subscriptionid-enable) | Resume matching for a subscription |
| [POST `/v1/projects/{projectId}/subscriptions/{subscriptionId}/disable`](#post-v1-projects-projectid-subscriptions-subscriptionid-disable) | Stop matching, keep the filter |

### GET `/v1/projects/{projectId}/subscriptions`

**List the subscriptions in a project**

Newest first. `event_types` comes back exactly as it was stored. The envelope is `{ data, has_more, next_offset }` - there is no `total`, because it would cost a second COUNT on every request and a client pages on `has_more`. Read `has_more` before concluding you have seen every route out of this project, and pass `next_offset` back as `offset` to continue.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |
| `endpoint_id` | query | string | no | max 64 chars | Only subscriptions bound to this endpoint. The id is resolved through the tenant scope first, so an endpoint belonging to another customer answers 404 rather than an empty list - the same answer an id that does not exist gets. |
| `enabled` | query | boolean | no |  | Filter by operator intent. Omit to see both enabled and disabled. |
| `limit` | query | number | no | 1 to 200 |  |
| `offset` | query | number | no | min 0 |  |

#### Responses

**200 OK** - [SubscriptionListDto](./schemas.md#subscriptionlistdto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `data` | [SubscriptionDto](./schemas.md#subscriptiondto)[] | yes |  |  |
| `data[].id` | string | yes |  |  |
| `data[].project_id` | string | yes |  |  |
| `data[].endpoint_id` | string | yes |  |  |
| `data[].name` | string \| null | yes |  |  |
| `data[].event_types` | string[] | yes |  | Exactly as stored. Never rewritten, never widened, never defaulted. |
| `data[].payload_filter` | object \| null | yes |  | The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. |
| `data[].enabled` | boolean | yes |  | A disabled subscription matches no events at all. |
| `data[].created_at` | string | yes |  |  |
| `data[].updated_at` | string | yes |  |  |
| `has_more` | boolean | yes |  | More subscriptions match than this page carries. |
| `next_offset` | number \| null | yes |  | Pass back as `offset` for the next page. Null when this page was the last. |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)

### POST `/v1/projects/{projectId}/subscriptions`

**Subscribe an endpoint to a set of event types**

The filter is stored EXACTLY as sent. A pattern the router cannot honour is a 400 - it is never coerced to "*", and an empty `event_types` array is refused rather than guessed at. Subject to a per-project ceiling (`MAX_SUBSCRIPTIONS_PER_PROJECT`), enforced inside a serializable transaction because each subscription multiplies the deliveries one event produces.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |

#### Request body

`application/json`, required - [CreateSubscriptionDto](./schemas.md#createsubscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string \| null | no | max 200 chars | For humans reading the subscription list. Optional; the column is nullable. |
| `endpoint_id` | string | yes | max 64 chars | An endpoint in THIS project. Resolved through the tenant scope, so an id belonging to another customer answers 404 with the same message as an id that does not exist. |
| `event_types` | string[] | yes | 1 to 100 items | The event types this subscription receives. Exactly three forms are accepted, and they are the three the router implements: "*" (everything), "payment.*" (every type beginning with the literal "payment.", so NOT "payments.settled" and NOT "payment"), and an exact type. Anything else is REFUSED - never widened to "*", never stored as something the router would read differently. An empty array is refused too: it would match no events at all. "*" may not appear alongside other patterns. |
| `payload_filter` | object \| null | no |  | A JSON predicate applied to the event body after the event-type filter matches. Implicit AND over field paths; $and/$or/$not combine filters; $eq/$ne/$gt/$gte/$lt/$lte/$in/$nin/$exists compare one path. Strict JSON typing, no coercion; an absent path satisfies only {"$exists": false}. Bounded to 4096 bytes, 5 levels and 64 conditions. NOT YET EVALUATED BY THE DATA PLANE - a stored filter is inert until the router implements it. Send null, or omit it, for no body filter; an empty object is refused because it would match everything. |
| `enabled` | boolean | no | default `true` | A disabled subscription never matches: it is skipped before its event-type filter is even evaluated. This is how you stop deliveries without touching the filter. |

#### Responses

**201 Created** - [SubscriptionDto](./schemas.md#subscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `endpoint_id` | string | yes |  |  |
| `name` | string \| null | yes |  |  |
| `event_types` | string[] | yes |  | Exactly as stored. Never rewritten, never widened, never defaulted. |
| `payload_filter` | object \| null | yes |  | The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. |
| `enabled` | boolean | yes |  | A disabled subscription matches no events at all. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**400 Bad Request** - [ApiErrorResponse](./schemas.md#apierrorresponse)

An event-type pattern is not one of "*", "prefix.*" or an exact type; `event_types` is empty; "*" appears alongside other patterns; or `payload_filter` is not a valid, bounded predicate. `details.field` names which.

`error.code`: [`invalid_request`](./errors.md#invalid-request)

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Two different 409s, told apart by `error.code`, never by the message. `limit_exceeded` - the project is at its subscription ceiling; `details` carries `{ limit, current, resource }`. `conflict` - the endpoint named by `endpoint_id` has been deleted.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)

### GET `/v1/projects/{projectId}/subscriptions/{subscriptionId}`

**Fetch one subscription**

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |
| `subscriptionId` | path | string | yes |  | Subscription id, `sub_…`. |

#### Responses

**200 OK** - [SubscriptionDto](./schemas.md#subscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `endpoint_id` | string | yes |  |  |
| `name` | string \| null | yes |  |  |
| `event_types` | string[] | yes |  | Exactly as stored. Never rewritten, never widened, never defaulted. |
| `payload_filter` | object \| null | yes |  | The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. |
| `enabled` | boolean | yes |  | A disabled subscription matches no events at all. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)

### PATCH `/v1/projects/{projectId}/subscriptions/{subscriptionId}`

**Update a subscription**

`event_types` and `payload_filter` are REPLACED wholesale, not merged. `enabled` is not writable here - enabling and disabling have their own routes so that pausing a route is a distinct, separately auditable act rather than a field riding along in a rename. Both sides of a filter change are recorded in the audit log.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |
| `subscriptionId` | path | string | yes |  | Subscription id, `sub_…`. |

#### Request body

`application/json`, required - [UpdateSubscriptionDto](./schemas.md#updatesubscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string \| null | no | max 200 chars | Null clears the name. |
| `endpoint_id` | string | no | max 64 chars | Re-point this subscription at a different endpoint in the same project. Resolved through the tenant scope; a deleted endpoint is refused. |
| `event_types` | string[] | no | 1 to 100 items | Replaces the whole filter - this is not a merge. The same three forms are accepted as on create, and the same refusals apply: an invalid pattern is a 400, never a silent widening to "*", and an empty array is refused. |
| `payload_filter` | object \| null | no |  | Replaces the whole predicate. Null removes it. See the create DTO. |

#### Responses

**200 OK** - [SubscriptionDto](./schemas.md#subscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `endpoint_id` | string | yes |  |  |
| `name` | string \| null | yes |  |  |
| `event_types` | string[] | yes |  | Exactly as stored. Never rewritten, never widened, never defaulted. |
| `payload_filter` | object \| null | yes |  | The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. |
| `enabled` | boolean | yes |  | A disabled subscription matches no events at all. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**400 Bad Request** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Same validation as create. `details.field` names it.

`error.code`: [`invalid_request`](./errors.md#invalid-request)

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**409 Conflict** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The new endpoint has been deleted.

`error.code`: [`conflict`](./errors.md#conflict), [`limit_exceeded`](./errors.md#limit-exceeded), [`idempotency_key_reused`](./errors.md#idempotency-key-reused)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)

### DELETE `/v1/projects/{projectId}/subscriptions/{subscriptionId}`

**Remove a subscription (hard delete)**

The row is really deleted, unlike endpoints and projects. Nothing has a foreign key to `webhook_subscriptions`, the delivery ledger keeps its own `endpoint_id` and `event_id`, and the whole routing rule is written to the audit log on the way out so a historical delivery's `subscription_id` stays explainable. Idempotent: deleting an already-deleted subscription still answers 204.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |
| `subscriptionId` | path | string | yes |  | Subscription id, `sub_…`. |

#### Responses

**204 No Content** - no body

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)

### POST `/v1/projects/{projectId}/subscriptions/{subscriptionId}/enable`

**Resume matching for a subscription**

Idempotent. The filter is untouched.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |
| `subscriptionId` | path | string | yes |  | Subscription id, `sub_…`. |

#### Responses

**200 OK** - [SubscriptionDto](./schemas.md#subscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `endpoint_id` | string | yes |  |  |
| `name` | string \| null | yes |  |  |
| `event_types` | string[] | yes |  | Exactly as stored. Never rewritten, never widened, never defaulted. |
| `payload_filter` | object \| null | yes |  | The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. |
| `enabled` | boolean | yes |  | A disabled subscription matches no events at all. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)

### POST `/v1/projects/{projectId}/subscriptions/{subscriptionId}/disable`

**Stop matching, keep the filter**

A disabled subscription is skipped before the event-type test, so it matches nothing at all. This - not an empty `event_types` array - is how you stop deliveries down one route. Deliveries already queued are not discarded. Idempotent; the reason goes to the audit log.

**Auth:** signed-in dashboard session (cookie).

#### Parameters

| Name | In | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| `projectId` | path | string | yes |  | Project id, `proj_…`. |
| `subscriptionId` | path | string | yes |  | Subscription id, `sub_…`. |

#### Request body

`application/json`, required - [DisableSubscriptionDto](./schemas.md#disablesubscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `reason` | string | no | max 200 chars | Recorded in the audit log so the delivery gap this creates can be explained later. |

#### Responses

**200 OK** - [SubscriptionDto](./schemas.md#subscriptiondto)

| Property | Type | Required | Constraints | Description |
|---|---|---|---|---|
| `id` | string | yes |  |  |
| `project_id` | string | yes |  |  |
| `endpoint_id` | string | yes |  |  |
| `name` | string \| null | yes |  |  |
| `event_types` | string[] | yes |  | Exactly as stored. Never rewritten, never widened, never defaulted. |
| `payload_filter` | object \| null | yes |  | The stored JSON predicate, or null. NOT YET EVALUATED by the data plane: a subscription with a payload filter currently behaves as if this were null. |
| `enabled` | boolean | yes |  | A disabled subscription matches no events at all. |
| `created_at` | string | yes |  |  |
| `updated_at` | string | yes |  |  |

**403 Forbidden** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Membership is proven but the role is short of the permission.

`error.code`: [`forbidden`](./errors.md#forbidden), [`email_not_verified`](./errors.md#email-not-verified)

**404 Not Found** - [ApiErrorResponse](./schemas.md#apierrorresponse)

The project, subscription or endpoint is not visible to this caller - absent, or another tenant's. Those answer identically, with one message, so this API cannot be used to confirm that an id scraped from a log or an old dashboard URL is live infrastructure belonging to another customer.

`error.code`: [`not_found`](./errors.md#not-found)

**429 Too Many Requests** - [ApiErrorResponse](./schemas.md#apierrorresponse)

Rate limited. Carries `Retry-After` (seconds) and `details.retry_after_seconds`. Only the write routes are limited; the listing and the fetch are not.

`error.code`: [`rate_limited`](./errors.md#rate-limited)
